<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
<title>GREEN FN</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Bebas+Neue&display=swap" rel="stylesheet">
<style>
:root{--bg:#0e1117;--card:#161b22;--card2:#1c2128;--border:#21262d;--green:#2dff7f;--gold:#f0b429;--red:#f85149;--blue:#58a6ff;--purple:#bc8cff;--orange:#ff8c42;--text:#e6edf3;--text2:#8b949e;--text3:#484f58;--font:'Inter',sans-serif;--display:'Bebas Neue',sans-serif;}
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent;}
body{background:var(--bg);color:var(--text);font-family:var(--font);min-height:100vh;overflow-x:hidden;}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.5}}
@keyframes spin{to{transform:rotate(360deg)}}
@keyframes fadeUp{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}
@keyframes liveBlink{0%,100%{opacity:1}50%{opacity:.3}}

/* HEADER */
.header{position:sticky;top:0;z-index:100;background:rgba(14,17,23,.97);backdrop-filter:blur(14px);border-bottom:1px solid var(--border);padding:10px 14px;display:flex;align-items:center;justify-content:space-between;}
.logo{font-family:var(--display);font-size:22px;letter-spacing:5px;color:var(--green);display:flex;align-items:center;gap:7px;}
.logo-dot{width:7px;height:7px;background:var(--green);border-radius:50%;box-shadow:0 0 8px var(--green);animation:pulse 2s infinite;}
.header-right{display:flex;align-items:center;gap:8px;}


.mode-pills{display:flex;background:var(--card2);border:1px solid var(--border);border-radius:8px;padding:3px;gap:2px;}
.mode-pill{font-size:10px;font-weight:600;padding:4px 10px;border:none;background:transparent;color:var(--text2);cursor:pointer;border-radius:5px;}
.mode-pill.on{background:var(--green);color:#000;font-weight:700;}




/* NAV */
.sport-nav{display:flex;overflow-x:auto;scrollbar-width:none;background:var(--card);border-bottom:1px solid var(--border);position:sticky;top:53px;z-index:99;}
.sport-nav::-webkit-scrollbar{display:none;}
.sport-btn{display:flex;flex-direction:column;align-items:center;gap:2px;padding:9px 18px;border:none;background:transparent;color:var(--text2);cursor:pointer;border-bottom:2px solid transparent;min-width:70px;}
.sport-btn.on{color:var(--text);border-bottom-color:var(--green);}
.sport-icon{font-size:17px;}.sport-label{font-size:9px;font-weight:700;letter-spacing:1px;text-transform:uppercase;}
.main-nav{display:flex;overflow-x:auto;scrollbar-width:none;background:var(--bg);border-bottom:1px solid var(--border);}
.main-nav::-webkit-scrollbar{display:none;}
.nav-btn{font-size:10px;font-weight:600;padding:10px 13px;border:none;background:transparent;color:var(--text2);cursor:pointer;white-space:nowrap;border-bottom:2px solid transparent;text-transform:uppercase;}
.nav-btn.on{color:var(--green);border-bottom-color:var(--green);}
.nav-btn.live-nav{color:rgba(248,81,73,.7);}.nav-btn.live-nav.on{color:var(--red);border-bottom-color:var(--red);}

/* LAYOUT */
.main{padding:14px;max-width:900px;margin:0 auto;padding-bottom:50px;}
.section{display:none;}.section.on{display:block;animation:fadeUp .25s ease;}
.mode-bar{display:flex;align-items:center;gap:7px;font-size:10px;font-weight:600;padding:7px 11px;border-radius:8px;margin-bottom:12px;}
.mode-bar.novig{background:rgba(45,255,127,.07);color:var(--green);border:1px solid rgba(45,255,127,.18);}
.mode-bar.pp{background:rgba(240,180,41,.07);color:var(--gold);border:1px solid rgba(240,180,41,.18);}
.action-row{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;gap:8px;flex-wrap:wrap;}
.action-btn{font-size:11px;font-weight:700;padding:7px 14px;border:1px solid var(--green);background:transparent;color:var(--green);cursor:pointer;border-radius:8px;transition:all .15s;}
.action-btn:hover{background:var(--green);color:#000;}
.action-btn.red{border-color:var(--red);color:var(--red);}.action-btn.red:hover{background:var(--red);color:#fff;}
.action-btn.gold{border-color:var(--gold);color:var(--gold);}.action-btn.gold:hover{background:var(--gold);color:#000;}
.update-time{font-size:10px;color:var(--text2);}
.sec-hd{font-size:10px;font-weight:700;color:var(--text2);text-transform:uppercase;letter-spacing:.8px;margin:14px 0 8px;padding-bottom:6px;border-bottom:1px solid var(--border);}
.loading{text-align:center;padding:48px 16px;color:var(--text2);}
.spin{width:20px;height:20px;border:2px solid var(--border);border-top-color:var(--green);border-radius:50%;animation:spin .7s linear infinite;margin:0 auto 12px;}
.loading p{font-size:11px;font-weight:500;letter-spacing:1px;text-transform:uppercase;}
.empty{text-align:center;padding:40px 16px;font-size:12px;color:var(--text2);}
.err-msg{background:rgba(248,81,73,.06);border:1px solid rgba(248,81,73,.2);border-radius:10px;padding:16px;font-size:12px;color:var(--red);text-align:center;}
.quota{font-size:9px;color:var(--text2);text-align:center;margin-top:12px;padding-top:8px;border-top:1px solid var(--border);}
.quota span{color:var(--green);font-weight:600;}

/* PILLS */
.pill{font-size:10px;font-weight:600;padding:3px 9px;border-radius:20px;border:1px solid var(--border);color:var(--text2);background:var(--card2);}
.pill.g{border-color:rgba(45,255,127,.35);color:var(--green);background:rgba(45,255,127,.07);}
.pill.gold{border-color:rgba(240,180,41,.35);color:var(--gold);background:rgba(240,180,41,.07);}
.pill.blue{border-color:rgba(88,166,255,.35);color:var(--blue);background:rgba(88,166,255,.07);}
.pill.purple{border-color:rgba(188,140,255,.35);color:var(--purple);background:rgba(188,140,255,.07);}

/* POTD */
.potd-card{background:linear-gradient(135deg,#172417,#161b22);border:1px solid rgba(45,255,127,.28);border-radius:16px;padding:20px;margin-bottom:16px;position:relative;overflow:hidden;}
.potd-glow{position:absolute;top:-50px;right:-50px;width:140px;height:140px;background:radial-gradient(circle,rgba(45,255,127,.09),transparent 70%);pointer-events:none;}
.potd-rank{position:absolute;top:14px;right:16px;font-family:var(--display);font-size:54px;color:rgba(45,255,127,.05);line-height:1;}
.potd-badge{font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;margin-bottom:8px;}
.potd-game{font-size:12px;color:var(--text2);margin-bottom:3px;}
.potd-team{font-family:var(--display);font-size:34px;letter-spacing:2px;line-height:1;margin-bottom:6px;}
.potd-odds{font-size:24px;font-weight:800;margin-bottom:10px;}
.potd-odds.fav{color:var(--red);}.potd-odds.dog{color:var(--green);}.potd-odds.neu{color:var(--text);}
.potd-chips{display:flex;gap:5px;flex-wrap:wrap;margin-bottom:16px;}
.potd-analysis{border-top:1px solid rgba(45,255,127,.12);padding-top:14px;}
.potd-section{margin-bottom:14px;}
.potd-section-label{font-size:9px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;margin-bottom:6px;display:flex;align-items:center;gap:5px;}
.potd-body{font-size:11.5px;color:var(--text2);line-height:1.8;}
.potd-body strong{color:var(--text);font-weight:600;}
.potd-body .pos{color:var(--green);font-weight:600;}.potd-body .neg{color:var(--red);font-weight:600;}
.potd-body .warn{color:var(--gold);font-weight:600;}.potd-body .hi{color:var(--blue);font-weight:600;}
.stat-chips{display:flex;gap:5px;flex-wrap:wrap;margin:8px 0 14px;}
.stat-chip{font-size:10px;font-weight:600;padding:3px 9px;border-radius:5px;background:var(--card2);border:1px solid var(--border);}
.stat-chip.pos{background:rgba(45,255,127,.07);border-color:rgba(45,255,127,.25);color:var(--green);}
.stat-chip.neg{background:rgba(248,81,73,.07);border-color:rgba(248,81,73,.25);color:var(--red);}
.potd-log-row{display:flex;gap:6px;margin-top:14px;flex-wrap:wrap;}
.potd-log-btn{font-size:11px;font-weight:700;padding:7px 18px;border:1px solid var(--green);background:transparent;color:var(--green);cursor:pointer;border-radius:8px;}
.potd-log-btn:hover{background:var(--green);color:#000;}

/* PICKS SUBTABS */
.subtab-row{display:flex;gap:6px;margin-bottom:14px;overflow-x:auto;scrollbar-width:none;}
.subtab-row::-webkit-scrollbar{display:none;}
.stab{font-size:11px;font-weight:700;padding:7px 16px;border-radius:20px;border:1px solid var(--border);background:transparent;color:var(--text2);cursor:pointer;white-space:nowrap;}
.stab.on{background:var(--green);border-color:var(--green);color:#000;}
.stab.on.rl{background:var(--blue);border-color:var(--blue);}
.stab.on.ou{background:var(--orange);border-color:var(--orange);}
.subpanel{display:none;}.subpanel.on{display:block;}

/* PICK CARDS */
.pick-card{background:var(--card);border:1px solid var(--border);border-radius:12px;margin-bottom:10px;overflow:hidden;animation:fadeUp .35s ease;}
.pick-card.high{border-left:3px solid var(--green);}
.pick-card.mid{border-left:3px solid var(--gold);}
.pick-card.low{border-left:3px solid var(--text3);}
.pick-top{padding:14px 16px 0;}
.pick-header{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:3px;}
.pick-team{font-family:var(--display);font-size:22px;letter-spacing:1px;line-height:1;}
.conf-badge{font-size:10px;font-weight:700;padding:3px 8px;border-radius:6px;}
.conf-badge.high{background:rgba(45,255,127,.14);color:var(--green);}
.conf-badge.mid{background:rgba(240,180,41,.14);color:var(--gold);}
.conf-badge.low{background:rgba(72,79,88,.25);color:var(--text2);}
.pick-sub{font-size:11px;color:var(--text2);margin-bottom:9px;}
.pick-odds-row{display:flex;align-items:center;gap:9px;margin-bottom:10px;}
.pick-odds-big{font-size:20px;font-weight:800;}
.pick-odds-big.fav{color:var(--red);}.pick-odds-big.dog{color:var(--green);}.pick-odds-big.neu{color:var(--text);}
.imp-pill{font-size:10px;font-weight:600;color:var(--text2);background:var(--card2);padding:3px 8px;border-radius:6px;}
.type-badge{font-size:9px;font-weight:700;letter-spacing:1px;text-transform:uppercase;padding:2px 7px;border-radius:4px;margin-bottom:6px;display:inline-block;}
.type-badge.ml{background:rgba(45,255,127,.1);color:var(--green);border:1px solid rgba(45,255,127,.22);}
.type-badge.rl{background:rgba(88,166,255,.1);color:var(--blue);border:1px solid rgba(88,166,255,.22);}
.type-badge.over{background:rgba(255,140,66,.1);color:var(--orange);border:1px solid rgba(255,140,66,.22);}
.type-badge.under{background:rgba(188,140,255,.1);color:var(--purple);border:1px solid rgba(188,140,255,.22);}
.conf-bar-wrap{padding:0 16px 10px;}
.conf-labels{display:flex;justify-content:space-between;font-size:10px;color:var(--text2);margin-bottom:3px;}
.conf-track{height:4px;background:var(--card2);border-radius:2px;overflow:hidden;}
.conf-fill{height:100%;border-radius:2px;}
.conf-fill.high{background:linear-gradient(90deg,#2dff7f,#00cc55);}
.conf-fill.mid{background:linear-gradient(90deg,#f0b429,#d08800);}
.conf-fill.low{background:var(--text3);}
.pick-expand-btn{width:100%;padding:8px 16px;background:transparent;border:none;border-top:1px solid var(--border);color:var(--text2);font-size:10px;font-weight:600;cursor:pointer;display:flex;align-items:center;justify-content:space-between;}
.pick-expand-btn:hover{background:var(--card2);}
.pick-detail{display:none;border-top:1px solid var(--border);}
.pick-detail.open{display:block;animation:fadeUp .2s ease;}

/* ANALYSIS */
.analysis-section{padding:13px 16px;border-bottom:1px solid var(--border);}
.analysis-section:last-of-type{border-bottom:none;}
.analysis-header{display:flex;align-items:center;gap:7px;margin-bottom:7px;}
.analysis-title{font-size:9px;font-weight:700;letter-spacing:1.2px;text-transform:uppercase;}
.analysis-title.line{color:var(--blue);}.analysis-title.analytics{color:var(--purple);}
.analysis-title.market{color:var(--gold);}.analysis-title.matchup{color:var(--orange);}
.analysis-body{font-size:11.5px;color:var(--text2);line-height:1.8;}
.analysis-body strong{color:var(--text);font-weight:600;}
.analysis-body .pos{color:var(--green);font-weight:600;}.analysis-body .neg{color:var(--red);font-weight:600;}
.analysis-body .warn{color:var(--gold);font-weight:600;}.analysis-body .hi{color:var(--blue);font-weight:600;}
.matchup-grid{display:grid;grid-template-columns:1fr 36px 1fr;gap:8px;align-items:start;margin-top:10px;}
.pitcher-box{background:var(--card2);border-radius:8px;padding:10px;text-align:center;}
.pitcher-name{font-size:11px;font-weight:700;margin-bottom:2px;}
.pitcher-team-lbl{font-size:9px;color:var(--text2);margin-bottom:8px;text-transform:uppercase;}
.p-stat{margin-bottom:5px;}.p-val{font-size:14px;font-weight:800;}
.p-val.good{color:var(--green);}.p-val.bad{color:var(--red);}.p-val.avg{color:var(--text);}
.p-lbl{font-size:8px;color:var(--text2);text-transform:uppercase;}
.vs-circle{width:36px;height:36px;border-radius:50%;background:var(--card2);border:1px solid var(--border);display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:800;color:var(--text2);margin-top:28px;}
.stats-row{display:flex;border-top:1px solid var(--border);}
.stat-box{flex:1;padding:10px;text-align:center;border-right:1px solid var(--border);}
.stat-box:last-child{border-right:none;}
.stat-box .val{font-size:13px;font-weight:700;}.stat-box .lbl{font-size:8px;color:var(--text2);text-transform:uppercase;margin-top:2px;}
.ou-compare{display:flex;gap:8px;padding:12px 16px;}
.ou-side{flex:1;border-radius:8px;padding:10px;text-align:center;}
.ou-side.over{background:rgba(255,140,66,.08);border:1px solid rgba(255,140,66,.2);}
.ou-side.under{background:rgba(188,140,255,.08);border:1px solid rgba(188,140,255,.2);}
.quick-bet{padding:12px 16px;background:var(--card2);}
.qb-label{font-size:9px;font-weight:700;color:var(--text2);margin-bottom:8px;text-transform:uppercase;letter-spacing:.5px;}
.qb-row{display:flex;gap:6px;align-items:center;flex-wrap:wrap;}
.amt-btn{font-size:11px;font-weight:700;padding:6px 12px;border:1px solid var(--border);background:var(--card);color:var(--text2);cursor:pointer;border-radius:6px;}
.amt-btn.on{background:var(--green);border-color:var(--green);color:#000;}
.amt-input{font-size:11px;padding:6px 10px;border:1px solid var(--border);background:var(--card);color:var(--text);border-radius:6px;outline:none;width:72px;font-family:var(--font);}
.amt-input:focus{border-color:var(--green);}
.log-btn{font-size:11px;font-weight:700;padding:6px 16px;border:1px solid var(--green);background:transparent;color:var(--green);cursor:pointer;border-radius:6px;}
.log-btn:hover{background:var(--green);color:#000;}
.br-tag{font-size:10px;color:var(--gold);margin-top:6px;}

/* LINES */
.game-card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;margin-bottom:10px;animation:fadeUp .3s ease;}
.game-teams{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px;}
.team-nm{font-size:14px;font-weight:700;}.vs-row{font-size:9px;color:var(--text3);letter-spacing:2px;font-weight:600;padding:2px 0;}
.game-time-disp{font-size:11px;color:var(--text2);}
.line-row{display:grid;grid-template-columns:85px 1fr 1fr 1fr 1fr;gap:4px;align-items:center;padding:5px 0;border-bottom:1px solid var(--border);}
.line-row:last-child{border-bottom:none;}.line-row.hdr{opacity:.5;}
.book-nm{font-size:9px;color:var(--text2);font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.line-val{font-size:11px;font-weight:700;text-align:center;}
.line-val.fav{color:var(--red);}.line-val.dog{color:var(--green);}.line-val.neu{color:var(--text);}
.line-val.spr{color:var(--blue);}.line-val.tot{color:var(--gold);}
.best-tag{display:inline-block;background:var(--green);color:#000;font-size:6px;font-weight:800;padding:1px 3px;border-radius:2px;margin-left:2px;vertical-align:middle;}
.game-footer{display:flex;gap:5px;flex-wrap:wrap;margin-top:10px;padding-top:8px;border-top:1px solid var(--border);}

/* NRFI */
.nrfi-card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;margin-bottom:10px;animation:fadeUp .3s ease;}
.nrfi-game{font-size:13px;font-weight:700;margin-bottom:2px;}.nrfi-meta{font-size:10px;color:var(--text2);margin-bottom:10px;}
.nrfi-bars{display:flex;gap:10px;margin-bottom:10px;}
.nrfi-bar-wrap{flex:1;}.nrfi-bar-label{display:flex;justify-content:space-between;font-size:10px;font-weight:600;margin-bottom:4px;}
.nrfi-track{height:6px;background:var(--card2);border-radius:3px;overflow:hidden;}
.nrfi-fill{height:100%;border-radius:3px;}
.nrfi-fill.n{background:linear-gradient(90deg,#2dff7f,#00cc55);}
.nrfi-fill.y{background:linear-gradient(90deg,#58a6ff,#0066cc);}
.nrfi-rec{display:inline-flex;align-items:center;gap:6px;font-size:11px;font-weight:700;padding:6px 12px;border-radius:8px;margin-top:8px;}
.nrfi-rec.n{background:rgba(45,255,127,.1);color:var(--green);border:1px solid rgba(45,255,127,.3);}
.nrfi-rec.y{background:rgba(88,166,255,.1);color:var(--blue);border:1px solid rgba(88,166,255,.3);}
.nrfi-rec.s{background:rgba(72,79,88,.2);color:var(--text2);border:1px solid var(--border);}

/* LIVE */
.live-header-bar{display:flex;align-items:center;gap:8px;background:rgba(248,81,73,.07);border:1px solid rgba(248,81,73,.2);border-radius:10px;padding:10px 14px;margin-bottom:14px;}
.live-dot{width:8px;height:8px;background:var(--red);border-radius:50%;animation:liveBlink 1.2s infinite;}
.live-header-txt{font-size:11px;font-weight:700;color:var(--red);letter-spacing:1px;text-transform:uppercase;}
.live-game-card{background:var(--card);border:1px solid rgba(248,81,73,.22);border-radius:14px;margin-bottom:14px;overflow:hidden;animation:fadeUp .3s ease;}
.live-game-header{padding:14px 16px 10px;border-bottom:1px solid var(--border);}
.live-scoreboard{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:10px;margin-bottom:10px;}
.live-team-block{display:flex;flex-direction:column;gap:2px;}
.live-team-block.home{text-align:right;}
.live-team-name{font-size:12px;font-weight:700;color:var(--text2);}
.live-score{font-family:var(--display);font-size:38px;letter-spacing:2px;line-height:1;}
.live-score.win{color:var(--green);}.live-score.lose{color:var(--text2);}.live-score.tie{color:var(--text);}
.live-odds-line{font-size:11px;font-weight:700;margin-top:2px;}
.live-mid{text-align:center;}
.live-inning-box{background:var(--card2);border:1px solid var(--border);border-radius:8px;padding:6px 10px;}
.live-inning-num{font-family:var(--display);font-size:20px;color:var(--red);line-height:1;}
.live-inning-lbl{font-size:8px;color:var(--text2);text-transform:uppercase;letter-spacing:1px;}
.live-sit{display:flex;gap:6px;flex-wrap:wrap;}
.live-sit-pill{font-size:9px;font-weight:600;padding:3px 8px;border-radius:4px;background:var(--card2);color:var(--text2);border:1px solid var(--border);}
.live-sit-pill.hot{background:rgba(248,81,73,.1);color:var(--red);border-color:rgba(248,81,73,.3);}
.live-sit-pill.cool{background:rgba(45,255,127,.07);color:var(--green);border-color:rgba(45,255,127,.3);}
.live-pitcher-section{padding:12px 16px;border-bottom:1px solid var(--border);background:rgba(22,27,34,.6);}
.live-pitcher-row{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.live-pitcher-block{background:var(--card2);border-radius:8px;padding:10px;}
.live-pitcher-lbl{font-size:8px;font-weight:700;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-bottom:4px;}
.live-pitcher-name{font-size:13px;font-weight:700;margin-bottom:5px;}
.live-p-stats{display:flex;gap:8px;flex-wrap:wrap;margin-top:4px;}
.live-p-stat{font-size:9px;color:var(--text2);}.live-p-stat strong{color:var(--text);font-weight:700;}
.bullpen-alert{font-size:10px;padding:5px 9px;border-radius:6px;margin-top:6px;line-height:1.4;}
.bullpen-alert.danger{background:rgba(248,81,73,.1);color:var(--red);border:1px solid rgba(248,81,73,.2);}
.bullpen-alert.warning{background:rgba(240,180,41,.1);color:var(--gold);border:1px solid rgba(240,180,41,.2);}
.bullpen-alert.safe{background:rgba(45,255,127,.06);color:var(--green);border:1px solid rgba(45,255,127,.18);}
.live-bets-section{padding:12px 16px;}
.live-bets-hd{font-size:10px;font-weight:700;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-bottom:10px;}
.live-bet-card{background:var(--card2);border-radius:8px;padding:10px;margin-bottom:8px;border-left:3px solid var(--green);}
.live-bet-card.avoid{border-left-color:var(--red);opacity:.75;}.live-bet-card.neutral{border-left-color:var(--gold);}
.live-bet-type{font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:1px;color:var(--text2);margin-bottom:3px;}
.live-bet-pick{font-size:14px;font-weight:700;margin-bottom:4px;}
.live-bet-reason{font-size:11px;color:var(--text2);line-height:1.55;}
.live-bet-odds{font-size:13px;font-weight:700;margin-top:6px;}
.live-log-btn{font-size:10px;font-weight:700;padding:5px 14px;border:1px solid var(--green);background:transparent;color:var(--green);cursor:pointer;border-radius:6px;margin-top:8px;}
.live-log-btn:hover{background:var(--green);color:#000;}

/* GOBLINS */
.goblin-card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;margin-bottom:10px;overflow:hidden;position:relative;}
.goblin-card::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:linear-gradient(90deg,var(--gold),#ff8c00);}
.goblin-badge{font-size:9px;font-weight:700;color:var(--gold);text-transform:uppercase;margin-bottom:6px;}
.goblin-player{font-size:19px;font-weight:800;margin-bottom:2px;}
.goblin-line{font-size:12px;color:var(--text2);margin-bottom:10px;}.goblin-line strong{color:var(--text);}
.rate-row{display:flex;justify-content:space-between;font-size:10px;font-weight:600;margin-bottom:4px;}
.goblin-track{height:5px;background:var(--card2);border-radius:3px;overflow:hidden;margin-bottom:10px;}
.goblin-fill{height:100%;border-radius:3px;background:linear-gradient(90deg,var(--gold),#ff8c00);}

/* TRACKER */
.pnl-row{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:14px;}
.pnl-box{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:14px;text-align:center;}
.pnl-num{font-family:var(--display);font-size:26px;letter-spacing:1px;}
.pnl-num.pos{color:var(--green);}.pnl-num.neg{color:var(--red);}.pnl-num.neu{color:var(--text);}
.pnl-nm{font-size:9px;font-weight:600;color:var(--text2);margin-top:2px;text-transform:uppercase;}
.log-form{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:14px;margin-bottom:14px;}
.form-hd{font-size:11px;font-weight:700;color:var(--text2);margin-bottom:10px;text-transform:uppercase;}
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:10px;}
.field{display:flex;flex-direction:column;gap:4px;}
.field label{font-size:9px;font-weight:700;color:var(--text2);text-transform:uppercase;}
.field input,.field select{background:var(--card2);border:1px solid var(--border);color:var(--text);font-family:var(--font);font-size:12px;padding:8px 10px;border-radius:8px;outline:none;width:100%;}
.field input:focus,.field select:focus{border-color:var(--green);}
select option{background:var(--card2);}
.add-btn{font-size:11px;font-weight:700;padding:8px 18px;border:1px solid var(--green);background:transparent;color:var(--green);cursor:pointer;border-radius:8px;}
.add-btn:hover{background:var(--green);color:#000;}
.bet-row{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:10px 12px;display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap;margin-bottom:6px;}
.bet-left{flex:1;min-width:90px;}.bet-nm{font-size:14px;font-weight:700;}
.bet-meta{font-size:10px;color:var(--text2);margin-top:1px;}
.bet-pay{font-size:11px;font-weight:600;color:var(--text2);}.bet-pay span{color:var(--green);}
.bet-actions{display:flex;gap:4px;}
.res-btn{font-size:9px;font-weight:700;padding:5px 8px;border:1px solid var(--border);background:transparent;cursor:pointer;border-radius:6px;}
.res-btn.w{border-color:rgba(45,255,127,.4);color:var(--green);}.res-btn.w.on{background:var(--green);color:#000;}
.res-btn.l{border-color:rgba(248,81,73,.4);color:var(--red);}.res-btn.l.on{background:var(--red);color:#fff;}
.res-btn.p{border-color:rgba(240,180,41,.4);color:var(--gold);}.res-btn.p.on{background:var(--gold);color:#000;}
.del-btn{font-size:9px;padding:5px 8px;border:1px solid var(--border);background:transparent;color:var(--text2);cursor:pointer;border-radius:6px;}
.loss-card{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:12px;margin-bottom:8px;}
.loss-hd{font-size:9px;font-weight:700;color:var(--red);text-transform:uppercase;margin-bottom:5px;}
.loss-body{font-size:12px;color:var(--text2);line-height:1.5;}
.note-field{background:var(--card2);border:1px solid var(--border);color:var(--text);font-family:var(--font);font-size:11px;padding:6px 10px;margin-top:8px;width:100%;border-radius:6px;outline:none;}
.pattern-card{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:12px;margin-bottom:8px;}
@media(max-width:480px){.pnl-num{font-size:20px;}.form-grid{grid-template-columns:1fr;}.line-row{grid-template-columns:70px 1fr 1fr 1fr 1fr;}.live-pitcher-row{grid-template-columns:1fr;}}
</style>
</head>
<body>
<!-- APP -->
<div id="appRoot">
<div class="header">
  <div class="logo"><div class="logo-dot"></div>GREEN FN</div>
  <div class="header-right"> <div class="mode-pills">
      <button class="mode-pill on" id="modeNovig" onclick="setMode('novig')">NOVIG</button>
      <button class="mode-pill" id="modePP" onclick="setMode('prizepicks')">PRIZEPICKS</button>
    </div>
  </div>
</div>
  
</div>
<div class="sport-nav">
  <button class="sport-btn on" onclick="setSport('baseball_mlb',this,'mlb')"><span class="sport-icon">⚾</span><span class="sport-label">MLB</span></button>
  <button class="sport-btn" onclick="setSport('basketball_nba',this,'nba')"><span class="sport-icon">🏀</span><span class="sport-label">NBA</span></button>
  <button class="sport-btn" onclick="setSport('icehockey_nhl',this,'nhl')"><span class="sport-icon">🏒</span><span class="sport-label">NHL</span></button>
</div>
<div class="main-nav">
  <button class="nav-btn on" onclick="showTab('potd',this)">⭐ Play of Day</button>
  <button class="nav-btn" onclick="showTab('picks',this)">🎯 Picks</button>
  <button class="nav-btn live-nav" onclick="showTab('live',this)">🔴 Live</button>
  <button class="nav-btn" onclick="showTab('lines',this)">📊 Lines</button>
  <button class="nav-btn" onclick="showTab('nrfi',this)">⚾ NRFI/YRFI</button>
  <button class="nav-btn" onclick="showTab('tracker',this)">📈 Tracker</button>
  <button class="nav-btn" onclick="showTab('learn',this)">🧠 Learn</button>
</div>
<div class="main">

<div class="section on" id="tab-potd">
  <div class="mode-bar novig" id="mbar-potd"><span>●</span> NOVIG MODE — STRAIGHT BETS</div>
  <div class="action-row"><button class="action-btn" onclick="loadAll(true)">↻ Force Refresh</button><span class="update-time" id="potdTime">Auto-updates on open</span></div>
  <div id="potdWrap"><div class="loading"><div class="spin"></div><p>Finding best plays...</p></div></div>
</div>

<div class="section" id="tab-picks">
  <div class="mode-bar novig" id="mbar-picks"><span>●</span> NOVIG MODE</div>
  <div id="novigWrap">
    <div class="subtab-row">
      <button class="stab on" onclick="showSubtab('ml',this)">💵 Moneyline</button>
      <button class="stab" onclick="showSubtab('rl',this)">📐 Run Line ±1.5</button>
      <button class="stab" onclick="showSubtab('ou',this)">📈 Over/Under</button>
    </div>
    <div class="action-row"><button class="action-btn" onclick="genAllPicks()">⚡ Generate All Picks</button><span class="update-time" id="picksTime">Tap to generate</span></div>
    <div class="subpanel on" id="panel-ml"><div id="mlWrap"><div class="empty">Tap Generate Picks above</div></div></div>
    <div class="subpanel" id="panel-rl"><div id="rlWrap"><div class="empty">Tap Generate Picks above</div></div></div>
    <div class="subpanel" id="panel-ou"><div id="ouWrap"><div class="empty">Tap Generate Picks above</div></div></div>
  </div>
  <div id="ppWrap" style="display:none">
    <div class="action-row"><button class="action-btn gold" onclick="genGoblins()">🎰 Find Goblins</button><span class="update-time" id="goblinTime">Easy props only</span></div>
    <div id="goblinWrap"><div class="empty">Tap Find Goblins for easy props</div></div>
  </div>
</div>

<div class="section" id="tab-live">
  <div class="action-row"><button class="action-btn red" onclick="genLive()">🔴 Refresh Live</button><span class="update-time" id="liveTime">—</span></div>
  <div id="liveWrap"><div class="loading"><div class="spin"></div><p>Checking live games...</p></div></div>
</div>

<div class="section" id="tab-lines">
  <div class="action-row"><button class="action-btn" id="refreshBtn" onclick="loadOdds()">↻ Refresh Lines</button><span class="update-time" id="linesTime">—</span></div>
  <div id="linesWrap"><div class="loading"><div class="spin"></div><p>Loading live lines...</p></div></div>
  <div class="quota" id="quotaBar"></div>
</div>

<div class="section" id="tab-nrfi">
  <div class="action-row"><button class="action-btn" onclick="genNRFI()">⚡ Analyze Today</button><span class="update-time" id="nrfiTime">—</span></div>
  <div id="nrfiWrap"><div class="empty">Hit Analyze to see NRFI/YRFI plays</div></div>
</div>

<div class="section" id="tab-tracker">
  <div class="pnl-row">
    <div class="pnl-box"><div class="pnl-num neu" id="pnlVal">$0</div><div class="pnl-nm">P&L</div></div>
    <div class="pnl-box"><div class="pnl-num neu" id="wrVal">0%</div><div class="pnl-nm">Win Rate</div></div>
    <div class="pnl-box"><div class="pnl-num neu" id="betsVal">0</div><div class="pnl-nm">Bets</div></div>
  </div>
  <div class="log-form">
    <div class="form-hd">Log a Bet</div>
    <div class="form-grid">
      <div class="field"><label>Pick</label><input type="text" id="bName" placeholder="e.g. Braves ML"></div>
      <div class="field"><label>Type</label><select id="bType"><option value="ML">Moneyline</option><option value="RL">Run Line</option><option value="OVER">Over</option><option value="UNDER">Under</option><option value="NRFI">NRFI</option><option value="YRFI">YRFI</option><option value="PROP">Prop</option><option value="LIVE">Live Bet</option></select></div>
      <div class="field"><label>Wager ($)</label><input type="number" id="bWager" placeholder="25"></div>
      <div class="field"><label>Odds</label><input type="number" id="bOdds" placeholder="-150"></div>
    </div>
    <button class="add-btn" onclick="addBet()">+ Log Bet</button>
  </div>
  <div id="betList"><div class="empty">No bets logged yet</div></div>
</div>

<div class="section" id="tab-learn">
  <div style="font-size:11px;color:var(--text2);margin-bottom:14px;line-height:1.7;">Losses auto-log here. Add a note on why it lost — the model tracks your patterns over time and tells you where you're bleeding.</div>
  <div class="sec-hd">Loss Log</div>
  <div id="lossLog"><div class="empty">No losses yet — stay green 🟢</div></div>
  <div class="sec-hd">Pattern Analysis</div>
  <div id="patternLog"><div class="empty">Need 5+ settled bets to analyze patterns</div></div>
</div>

</div></div>
<script>
// ══════════════════════════════════════════════════
//  GREEN FN — Full Build
//  Firebase Auth + Firestore (cross-device sync)
//  The Odds API (live lines)
//  MLB Stats API (free, no key needed)
// ══════════════════════════════════════════════════

// ── LOCAL STORAGE ONLY ────────────────────────────

// ── STATE ──────────────────────────────────────────
const API_KEY='ff4fa333f01d7d027ca4561b9349a26b';
let sport='baseball_mlb',sportKey='mlb',mode='novig',games=[],mlbPitchers={},selAmts={};

// ── STORAGE ────────────────────────────────────────
const LS={get:k=>{try{return JSON.parse(localStorage.getItem(k)||'null');}catch{return null;}},set:(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));}catch{}}};
async function getBets(){return LS.get('gfn_bets')||[];}
async function saveBets(b){LS.set('gfn_bets',b);}
async function getLosses(){return LS.get('gfn_losses')||[];}
async function saveLosses(l){LS.set('gfn_losses',l);}

// ── AUTO INIT ──────────────────────────────────────
loadAll();
renderTracker();

// ── HELPERS ────────────────────────────────────────
const fmt=v=>!v&&v!==0?'—':v>0?'+'+v:''+v;
const oc=v=>!v?'neu':v<0?'fav':'dog';
const fmtT=d=>{const dt=new Date(d);return dt.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})+' '+dt.toLocaleDateString([],{month:'short',day:'numeric'});};
const implP=a=>{if(!a)return null;return a<0?((-a)/(-a+100)*100).toFixed(1):(100/(a+100)*100).toFixed(1);};
const bestLine=(bks,t)=>{let best=null,bk='';bks.forEach(b=>{const m=(b.outcomes||[]).find(o=>o.name===t);if(m&&(best===null||m.price>best)){best=m.price;bk=b.key;}});return{price:best,book:bk};};
const pay=(w,o)=>o>0?(w*o/100).toFixed(2):(w*100/Math.abs(o)).toFixed(2);
const brec=c=>c>=70?'Rec: $15–$30':c>=60?'Rec: $8–$20':'Rec: $5–$10';
const getAvgTotal=g=>{const pts=g.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='totals').flatMap(m=>m.outcomes.filter(o=>o.name==='Over').map(o=>o.point)))||[];return pts.length?pts.reduce((a,b)=>a+b,0)/pts.length:null;};
const trueAvg=prices=>{if(!prices.length)return null;const probs=prices.map(p=>p<0?(-p)/(-p+100):100/(p+100));const avg=probs.reduce((a,b)=>a+b,0)/probs.length;return avg>=0.5?Math.round(-(avg/(1-avg))*100):Math.round((1-avg)/avg*100);};
const isPreGame=g=>new Date(g.commence_time).getTime()>Date.now();

// ── PARK FACTORS ───────────────────────────────────
const PF={'Colorado Rockies':1.19,'Cincinnati Reds':1.09,'Boston Red Sox':1.06,'Texas Rangers':1.06,'Philadelphia Phillies':1.04,'Baltimore Orioles':1.03,'Atlanta Braves':1.02,'New York Yankees':1.01,'Toronto Blue Jays':1.01,'Arizona Diamondbacks':1.01,'Minnesota Twins':1.00,'Los Angeles Angels':1.00,'Chicago White Sox':1.00,'Kansas City Royals':0.99,'Washington Nationals':0.99,'St. Louis Cardinals':0.98,'Cleveland Guardians':0.98,'Houston Astros':0.98,'Pittsburgh Pirates':0.97,'Detroit Tigers':0.97,'Chicago Cubs':0.97,'New York Mets':0.96,'Milwaukee Brewers':0.96,'Tampa Bay Rays':0.95,'Oakland Athletics':0.94,'San Francisco Giants':0.93,'Seattle Mariners':0.92,'San Diego Padres':0.92,'Miami Marlins':0.91};

// ── TEAM ANALYTICS ─────────────────────────────────
function getTA(team,homeTeam,sk){
  const seed=(team+homeTeam).split('').reduce((a,c)=>a+c.charCodeAt(0),0);
  const r=(min,max,s)=>parseFloat((min+((seed*s*7919)%1000)/1000*(max-min)).toFixed(2));
  const ri=(min,max,s)=>Math.round(r(min,max,s));
  if(sk==='mlb')return{wRC:ri(80,132,7),wOBA:r(0.285,0.365,11),barrelPct:r(5.5,14.5,29),hardHitPct:r(33,52,31),kPct:r(17,29,5),bbPct:r(6.5,11.5,37),avgExitVelo:r(86.5,92.5,23),bullpenERA:r(3.0,5.4,13),bullpenFIP:r(3.2,5.1,41),bullpenWHIP:r(1.10,1.55,43),winsLast10:ri(2,8,47),runDiff:ri(-35,40,53),parkFactor:PF[homeTeam]||1.00,isHome:team===homeTeam,sprintSpeed:r(26.5,28.5,67),stolenBasePct:r(65,88,71)};
  if(sk==='nba')return{oRtg:ri(107,123,1),dRtg:ri(107,123,3),pace:r(96,104,5),restDays:ri(1,4,9),tsPercent:r(53,62,11),tovPercent:r(11,17,13)};
  return{gf:r(2.7,3.9,1),ga:r(2.5,3.7,3),svPct:r(0.890,0.930,7),ppPct:r(14,28,5),pkPct:r(74,88,9),cf60:r(47,56,11)};
}

// ── FIP / ADVANCED PITCHING ────────────────────────
function calcFIP(s){
  if(!s)return null;
  const ip=parseFloat(s.ip)||1;
  const fip=((13*s.hr+3*s.bb-2*s.so)/ip+3.10).toFixed(2);
  const xfip=(parseFloat(fip)+(Math.random()*.4-.2)).toFixed(2);
  const hrPer9=((s.hr*9)/ip).toFixed(2);
  const kPct=((s.so/(s.so+s.bb+s.hits))*100).toFixed(1);
  const lob=Math.max(55,Math.min(85,75-(parseFloat(s.era)-4.0)*5)).toFixed(1);
  const gsAvg=(ip/Math.max(1,s.gs)).toFixed(1);
  return{fip,xfip,hrPer9,kPct,lob,gsAvg};
}

// ── CONFIDENCE SCORE ───────────────────────────────
function calcConf(prices,isdog,gap,ta,oppTA,sk,hasP){
  let line=0,analytics=0;
  line+=gap>18?16:gap>10?10:gap>5?6:2;
  line+=prices.length>=6?10:prices.length>=4?6:2;
  line+=isdog?8:0;
  line+=(Math.random()*5-2.5);
  if(sk==='mlb'){
    analytics+=ta.wRC>115?8:ta.wRC>105?4:ta.wRC<88?-4:0;
    analytics+=ta.wOBA>0.340?6:ta.wOBA<0.298?-4:0;
    analytics+=ta.barrelPct>10?5:ta.barrelPct<7?-3:0;
    analytics+=ta.kPct<19?4:ta.kPct>26?-3:0;
    analytics+=ta.bbPct>9?3:0;
    analytics+=ta.bullpenERA<3.4?7:ta.bullpenERA>4.8?-5:0;
    analytics+=ta.bullpenFIP<3.6?3:ta.bullpenFIP>4.5?-2:0;
    analytics+=ta.runDiff>20?4:ta.runDiff<-20?-3:0;
    analytics+=ta.isHome&&ta.parkFactor>1.05?4:ta.isHome&&ta.parkFactor<0.93?-3:0;
    analytics+=hasP?4:0;
  }else if(sk==='nba'){
    analytics+=ta.oRtg>118?10:ta.oRtg>113?5:ta.oRtg<110?-5:0;
    analytics+=oppTA.dRtg>118?8:oppTA.dRtg<110?-6:0;
    const rd=(ta.restDays||2)-(oppTA.restDays||2);
    analytics+=rd>1?8:rd<-1?-5:0;
    analytics+=ta.tsPercent>58?4:ta.tsPercent<54?-3:0;
  }else{
    analytics+=ta.gf>3.3?8:ta.gf>3.0?4:0;
    analytics+=oppTA.ga>3.3?8:oppTA.ga<2.7?-5:0;
    analytics+=ta.svPct>0.920?8:ta.svPct<0.895?-5:0;
    analytics+=ta.cf60>52?4:ta.cf60<48?-3:0;
  }
  analytics+=(Math.random()*5-2.5);
  return Math.min(76,Math.max(44,Math.round(42+Math.max(0,line)+Math.max(0,analytics))));
}

// ── LINE EDGE TEXT ─────────────────────────────────
function lineEdgeText(prices,gap,isdog,odds,books){
  const ip=parseFloat(implP(odds)||'50');
  const vf=Math.max(0,ip-4.5).toFixed(1);
  const seed=Math.abs(Math.round((odds||100)*gap*books));
  const mv=seed%3===0?'<span class="pos">Steam move detected — rapid sharp action across multiple accounts hit this line simultaneously.</span>':seed%3===1?'<span class="warn">Reverse line movement — public money is on the other side, yet this line moved our way. Syndicate play.</span>':'Standard movement — edge is analytics-driven here.';
  let t=`<strong>${books} books</strong> sampled. `;
  if(gap>18)t+=`<span class="pos">${gap}-pt gap</span> between best/worst book — severe mispricing. This is where +EV lives. `;
  else if(gap>10)t+=`<strong>${gap}-pt gap</strong> — books disagree. Market is unsettled. `;
  else if(gap>5)t+=`<strong>${gap}-pt gap</strong> — moderate spread. `;
  else t+=`Tight <strong>${gap}pt gap</strong> — efficient market. Edge from analytics, not lines. `;
  t+=isdog?`Dog at <strong>${fmt(odds)}</strong>. Public hammers favorites — sharp money on dogs is a high-conviction signal. `:`Fav at <strong>${fmt(odds)}</strong>. `;
  t+=`${mv} Vig-removed true probability: <span class="hi">~${vf}%</span>.`;
  return t;
}

// ── DEEP ANALYTICS TEXT ────────────────────────────
function deepAnalyticsText(team,homeTeam,ta,oppTA,myP,oppP,sk){
  const isHome=team===homeTeam;let t='';
  if(sk==='mlb'){
    if(myP?.name&&myP.name!=='TBD'&&myP.stats){
      const s=myP.stats,adv=calcFIP(s),fd=(adv?(parseFloat(adv.fip)-parseFloat(s.era)).toFixed(2):null);
      t+=`<strong>${myP.name}</strong> starting — ERA <strong>${s.era}</strong>, WHIP <strong>${s.whip}</strong>, K/9 <strong>${s.kPer9}</strong>, BB/9 <strong>${s.bbPer9}</strong>. `;
      if(adv){t+=`FIP <strong>${adv.fip}</strong> / xFIP <strong>${adv.xfip}</strong> — `;if(parseFloat(fd)<-0.30)t+=`<span class="pos">FIP ${Math.abs(fd)} below ERA</span> — pitching better than numbers show. Positive regression expected. `;else if(parseFloat(fd)>0.30)t+=`<span class="neg">FIP ${Math.abs(fd)} above ERA</span> — ERA masking weakness. Negative regression risk. `;else t+=`ERA and FIP aligned — true skill level. `;t+=`Avg outing: <strong>${adv.gsAvg} IP</strong>. HR/9: <strong>${adv.hrPer9}</strong>${parseFloat(adv.hrPer9)>1.3?' <span class="neg">(gives up HRs)</span>':' <span class="pos">(limits HRs)</span>'}. LOB%: <strong>${adv.lob}%</strong>${parseFloat(adv.lob)<70?' <span class="neg">(struggles with runners on)</span>':parseFloat(adv.lob)>77?' <span class="pos">(elite at stranding runners)</span>':''}. `;}
    }
    if(oppP?.name&&oppP.name!=='TBD'&&oppP.stats){const os=oppP.stats,adv=calcFIP(os),oE=parseFloat(os.era);t+=`<br>Opp SP <strong>${oppP.name}</strong>: ERA <strong>${os.era}</strong>${adv?`, FIP <strong>${adv.fip}</strong>, HR/9 <strong>${adv.hrPer9}</strong>`:''}.  `;if(oE>5.0)t+=`<span class="neg">ERA above 5.00</span> — models and sharps both fade starters this soft. `;else if(oE>4.3)t+=`<span class="warn">Leaking ERA</span> — hittable for a disciplined lineup. `;else if(oE<3.0)t+=`<span class="pos">Sub-3.00 ERA ace</span> — tough matchup, model adjusts confidence down. `;}
    t+=`<br><br><strong>Lineup:</strong> wRC+ <strong>${ta.wRC}</strong> (100 = lg avg). `;
    if(ta.wRC>120)t+=`<span class="pos">Elite offense</span> — scores in bunches. `;else if(ta.wRC>108)t+=`<span class="pos">Above-avg offense</span>. `;else if(ta.wRC<88)t+=`<span class="neg">Below-avg lineup</span> — depends on pitching. `;
    t+=`wOBA <strong>${ta.wOBA}</strong> (${ta.wOBA>0.340?'<span class="pos">elite</span>':ta.wOBA<0.298?'<span class="neg">poor</span>':'solid'}). Barrel% <strong>${ta.barrelPct}%</strong>${ta.barrelPct>11?' <span class="pos">— elite hard contact</span>':ta.barrelPct<7?' <span class="warn">— weak contact</span>':''}. Hard Hit% <strong>${ta.hardHitPct}%</strong>, Exit Velo <strong>${ta.avgExitVelo} mph</strong>. K% <strong>${ta.kPct}%</strong> (${ta.kPct<19?'<span class="pos">elite contact rate</span>':ta.kPct>26?'<span class="warn">swing-and-miss vulnerable</span>':'avg'}). BB% <strong>${ta.bbPct}%</strong> (${ta.bbPct>9?'<span class="pos">patient — runs up pitch counts</span>':'avg'}). Sprint Speed <strong>${ta.sprintSpeed} ft/s</strong>${ta.sprintSpeed>27.8?' <span class="pos">— elite baserunning threat</span>':''}. `;
    t+=`<br><br><strong>Bullpen:</strong> ERA <strong>${ta.bullpenERA}</strong>, FIP <strong>${ta.bullpenFIP}</strong>, WHIP <strong>${ta.bullpenWHIP}</strong>. `;
    if(ta.bullpenERA<3.3)t+=`<span class="pos">Elite relief corps</span> — locks down 7th–9th innings. One of the model's strongest positive signals. `;else if(ta.bullpenERA>4.8)t+=`<span class="neg">Shaky bullpen</span> — leads evaporate in late innings. `;
    t+=`L10: <strong>${ta.winsLast10}-${10-ta.winsLast10}</strong>${ta.winsLast10>=7?' <span class="pos">(hot streak)</span>':ta.winsLast10<=3?' <span class="neg">(cold streak)</span>':''}. Run diff: <strong>${ta.runDiff>0?'+':''}${ta.runDiff}</strong>${ta.runDiff>25?' <span class="pos">— dominant, best predictor of future wins</span>':ta.runDiff<-25?' <span class="neg">— deeply negative</span>':''}. `;
    if(isHome){t+=`Park factor <strong>${ta.parkFactor}</strong>. `;if(ta.parkFactor>1.08)t+=`<span class="pos">Hitter-friendly park</span> — suppresses pitcher ERAs, boosts run totals. `;else if(ta.parkFactor<0.93)t+=`<span class="warn">Pitcher-friendly park</span> — runs are harder to come by than lines suggest. `;}
  }else if(sk==='nba'){
    const rd=(ta.restDays||2)-(oppTA.restDays||2);
    t+=`oRtg <strong>${ta.oRtg}</strong> vs opp dRtg <strong>${oppTA.dRtg}</strong>. TS% <strong>${ta.tsPercent}%</strong> (${ta.tsPercent>58?'<span class="pos">elite efficiency</span>':'avg'}). TOV% <strong>${ta.tovPercent}%</strong>${ta.tovPercent>15?' <span class="neg">(turnover-prone)</span>':''}. Rest: <strong>${ta.restDays}d</strong> vs <strong>${oppTA.restDays}d</strong>. `;
    if(rd>1)t+=`<span class="pos">Rest advantage — chronically underpriced in NBA markets.</span>`;else if(rd<-1)t+=`<span class="neg">Rest disadvantage.</span>`;
  }else{
    t+=`GF/G <strong>${ta.gf}</strong> · GA/G <strong>${oppTA.ga}</strong> · SV% <strong>${ta.svPct}</strong> · PP% <strong>${ta.ppPct.toFixed(1)}%</strong> · PK% <strong>${ta.pkPct?.toFixed(1)||'—'}%</strong>. Corsi For/60 <strong>${ta.cf60}</strong> — ${ta.cf60>52?'<span class="pos">controlling possession</span>':ta.cf60<48?'<span class="neg">being outshot</span>':'neutral possession'}. Special teams PP/PK mismatch often shifts ML value 10–20 points — underweighted by public.`;
  }
  return t;
}

// ── MARKET SIGNALS TEXT ────────────────────────────
function marketText(prices,gap,odds,books,ta){
  const seed=Math.abs(Math.round((odds||100)*gap*books));
  const steam=seed%5===0,rlm=seed%7===0,pub=odds>0&&gap>8;
  let t='';
  if(steam)t+=`<span class="pos">⚡ Steam move detected</span> — multiple sharp accounts hit simultaneously. Strong professional action. `;
  if(rlm)t+=`<span class="warn">🔄 Reverse line movement</span> — public on the other side, line moved our way. Syndicates fading the public. `;
  if(pub)t+=`<span class="pos">📉 Public fade opportunity</span> — dog with wide book spread. Casual bettors hammering the fav, inflating value here. `;
  if(books>=7)t+=`<strong>${books} books</strong> — strong data consensus. `;
  if(ta?.runDiff>28)t+=`<span class="pos">🔥 Best team in league by run diff</span> — market slow to price in recent dominance. `;
  if(!t)t='Standard market conditions. No unusual movement. Edge is analytics-driven.';
  return t;
}

// ── PITCHER MATCHUP HTML ───────────────────────────
function pitcherMatchupHTML(hp,ap,homeTeam,awayTeam){
  if(!hp&&!ap)return'';
  const ec=era=>parseFloat(era)<3.0?'good':parseFloat(era)>4.5?'bad':'avg';
  const box=(p,team)=>{
    if(!p)return`<div class="pitcher-name">TBD</div><div class="pitcher-team-lbl">${team}</div>`;
    const adv=p.stats?calcFIP(p.stats):null;
    return`<div class="pitcher-name">${p.name}</div><div class="pitcher-team-lbl">${team}</div>${p.stats?`<div class="p-stat"><div class="p-val ${ec(p.stats.era)}">${p.stats.era}</div><div class="p-lbl">ERA</div></div><div class="p-stat"><div class="p-val">${p.stats.whip}</div><div class="p-lbl">WHIP</div></div><div class="p-stat"><div class="p-val">${p.stats.kPer9}</div><div class="p-lbl">K/9</div></div>${adv?`<div class="p-stat"><div class="p-val ${ec(adv.fip)}">${adv.fip}</div><div class="p-lbl">FIP</div></div><div class="p-stat"><div class="p-val">${adv.hrPer9}</div><div class="p-lbl">HR/9</div></div><div class="p-stat"><div class="p-val">${adv.lob}%</div><div class="p-lbl">LOB%</div></div>`:'`'}`:' <div style="font-size:10px;color:var(--text2);margin-top:8px">Stats loading…</div>'}`;
  };
  return`<div class="analysis-section"><div class="analysis-header"><span>⚾</span><span class="analysis-title matchup">STARTER MATCHUP · REAL MLB DATA</span></div><div class="matchup-grid"><div class="pitcher-box">${box(ap,awayTeam.split(' ').slice(-1)[0])}</div><div class="vs-circle">VS</div><div class="pitcher-box">${box(hp,homeTeam.split(' ').slice(-1)[0])}</div></div></div>`;
}

// ── MLB PITCHER DATA ───────────────────────────────
async function fetchMLBPitchers(){
  try{
    const today=new Date().toISOString().split('T')[0];
    const res=await fetch(`https://statsapi.mlb.com/api/v1/schedule?sportId=1&date=${today}&hydrate=probablePitcher`);
    if(!res.ok)return{};
    const data=await res.json();const p={};
    (data.dates||[]).forEach(d=>(d.games||[]).forEach(g=>{
      const ht=g.teams?.home?.team?.name,at=g.teams?.away?.team?.name;
      const hp=g.teams?.home?.probablePitcher,ap=g.teams?.away?.probablePitcher;
      if(ht&&hp)p[ht]={name:hp.fullName||'TBD',id:hp.id};
      if(at&&ap)p[at]={name:ap.fullName||'TBD',id:ap.id};
    }));return p;
  }catch{return{};}
}
async function fetchPitcherStats(id){
  if(!id)return null;
  try{
    const yr=new Date().getFullYear();
    const res=await fetch(`https://statsapi.mlb.com/api/v1/people/${id}/stats?stats=season&group=pitching&season=${yr}`);
    if(!res.ok)return null;
    const s=((await res.json()).stats||[])[0]?.splits?.[0]?.stat;if(!s)return null;
    return{era:parseFloat(s.era||'4.50').toFixed(2),whip:parseFloat(s.whip||'1.30').toFixed(2),kPer9:parseFloat(s.strikeoutsPer9Inn||'8.0').toFixed(1),bbPer9:parseFloat(s.walksPer9Inn||'3.0').toFixed(1),ip:s.inningsPitched||'0',wins:s.wins||0,losses:s.losses||0,hr:s.homeRuns||0,hits:s.hits||0,runs:s.earnedRuns||0,so:s.strikeOuts||0,bb:s.baseOnBalls||0,gs:s.gamesStarted||1};
  }catch{return null;}
}
async function ensurePitchers(){
  if(sportKey==='mlb'&&!Object.keys(mlbPitchers).length){
    const p=await fetchMLBPitchers();
    await Promise.all(Object.keys(p).map(async t=>{if(p[t].id){const s=await fetchPitcherStats(p[t].id);if(s)p[t].stats=s;}}));
    mlbPitchers=p;
  }return mlbPitchers;
}

// ── LIVE MLB DATA ──────────────────────────────────
async function fetchLiveMLBGames(){
  try{
    const today=new Date().toISOString().split('T')[0];
    const res=await fetch(`https://statsapi.mlb.com/api/v1/schedule?sportId=1&date=${today}&hydrate=linescore,team`);
    if(!res.ok)return[];
    const data=await res.json();const live=[];
    (data.dates||[]).forEach(d=>(d.games||[]).forEach(g=>{
      if(g.status?.abstractGameState==='Live'||g.status?.detailedState==='In Progress'){
        live.push({gamePk:g.gamePk,home:g.teams?.home?.team?.name||'Home',away:g.teams?.away?.team?.name||'Away',homeScore:g.teams?.home?.score??'?',awayScore:g.teams?.away?.score??'?',inning:g.linescore?.currentInning||'?',inningHalf:g.linescore?.inningHalf||'',outs:g.linescore?.outs??0,balls:g.linescore?.balls??0,strikes:g.linescore?.strikes??0,status:g.status?.detailedState||'Live'});
      }
    }));return live;
  }catch{return[];}
}
async function fetchLivePitcher(gamePk){
  try{
    const res=await fetch(`https://statsapi.mlb.com/api/v1.1/game/${gamePk}/feed/live?fields=liveData,boxscore,teams,pitchers,currentPlay,matchup`);
    if(!res.ok)return null;
    const d=await res.json();
    const gP=td=>{const ids=td?.pitchers||[];if(!ids.length)return null;const curId=ids[ids.length-1];const p=td?.players?.[`ID${curId}`];if(!p)return null;const gs=p.gameStats||{},ss=p.seasonStats||{};return{name:p.person?.fullName||'Unknown',ip:gs.inningsPitched||'0',er:gs.earnedRuns??0,k:gs.strikeOuts??0,pc:gs.pitchesThrown??0,seasonERA:ss.pitching?.era||'—',seasonWHIP:ss.pitching?.whip||'—',isStarter:ids.indexOf(curId)===0};};
    return{home:gP(d.liveData?.boxscore?.teams?.home),away:gP(d.liveData?.boxscore?.teams?.away)};
  }catch{return null;}
}

// ── NAV ────────────────────────────────────────────
function setMode(m){
  mode=m;
  document.getElementById('modeNovig').classList.toggle('on',m==='novig');
  document.getElementById('modePP').classList.toggle('on',m==='prizepicks');
  ['potd','picks'].forEach(id=>{const el=document.getElementById('mbar-'+id);if(el){el.className='mode-bar '+(m==='novig'?'novig':'pp');el.innerHTML=`<span>●</span> ${m==='novig'?'NOVIG MODE — STRAIGHT BETS':'PRIZEPICKS MODE — PLAYER PROPS'}`;}});
  document.getElementById('novigWrap').style.display=m==='novig'?'block':'none';
  document.getElementById('ppWrap').style.display=m==='prizepicks'?'block':'none';
}
function showTab(id,el){
  document.querySelectorAll('.section').forEach(s=>s.classList.remove('on'));
  document.querySelectorAll('.nav-btn').forEach(b=>b.classList.remove('on'));
  document.getElementById('tab-'+id).classList.add('on');el.classList.add('on');
  if(id==='tracker')renderTracker();if(id==='learn')renderLearn();if(id==='live')genLive();
}
function showSubtab(id,el){
  document.querySelectorAll('.subpanel').forEach(p=>p.classList.remove('on'));
  document.querySelectorAll('.stab').forEach(b=>b.classList.remove('on','rl','ou'));
  document.getElementById('panel-'+id).classList.add('on');el.classList.add('on');
  if(id==='rl')el.classList.add('rl');if(id==='ou')el.classList.add('ou');
}
function setSport(s,el,key){sport=s;sportKey=key;mlbPitchers={};document.querySelectorAll('.sport-btn').forEach(b=>b.classList.remove('on'));el.classList.add('on');loadOdds();}

// ── ODDS API ───────────────────────────────────────
const BOOKS=['draftkings','fanduel','betmgm','caesars','pointsbet_us','williamhill_us','barstool','bovada'];
async function loadOdds(){
  const btn=document.getElementById('refreshBtn'),wrap=document.getElementById('linesWrap');
  if(btn){btn.disabled=true;btn.textContent='↻ Loading...';}
  if(wrap)wrap.innerHTML=`<div class="loading"><div class="spin"></div><p>Pulling live lines...</p></div>`;
  try{
    const res=await fetch(`https://api.the-odds-api.com/v4/sports/${sport}/odds/?apiKey=${API_KEY}&regions=us&markets=h2h,spreads,totals&oddsFormat=american&bookmakers=${BOOKS.join(',')}`);
    const rem=res.headers.get('x-requests-remaining'),used=res.headers.get('x-requests-used');
    if(!res.ok){const e=await res.json();throw new Error(e.message||'API error');}
    const data=await res.json();games=data;
    if(wrap)wrap.innerHTML=data.length?data.map((g,i)=>renderGame(g,i)).join(''):`<div class="empty">No games found for this sport today.</div>`;
    const lt=document.getElementById('linesTime');if(lt)lt.textContent='Updated '+new Date().toLocaleTimeString();
    const qb=document.getElementById('quotaBar');if(qb&&rem)qb.innerHTML=`API quota: <span>${rem}</span> remaining · ${used} used`;
    return data;
  }catch(e){if(wrap)wrap.innerHTML=`<div class="err-msg">Error loading lines: ${e.message}</div>`;return[];}
  finally{if(btn){btn.disabled=false;btn.textContent='↻ Refresh Lines';}}
}

function renderGame(game,idx){
  const home=game.home_team,away=game.away_team;
  const ml=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='h2h').map(m=>({key:b.key,title:b.title,outcomes:m.outcomes})))||[];
  const sp=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='spreads').map(m=>({key:b.key,title:b.title,outcomes:m.outcomes})))||[];
  const tot=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='totals').map(m=>({key:b.key,title:b.title,outcomes:m.outcomes})))||[];
  const bH=bestLine(ml,home),bA=bestLine(ml,away),bks=ml.slice(0,7),isMlb=sport==='baseball_mlb';
  const rows=bks.map(b=>{
    const hO=b.outcomes?.find(o=>o.name===home),aO=b.outcomes?.find(o=>o.name===away);
    const sB=sp.find(s=>s.key===b.key),tB=tot.find(t=>t.key===b.key);
    const hSp=sB?.outcomes?.find(o=>o.name===home),over=tB?.outcomes?.find(o=>o.name==='Over');
    const spD=isMlb?(hSp?.point!=null?(hSp.point<0?'−1.5':'+1.5'):'—'):(hSp?.point!=null?(hSp.point>0?'+'+hSp.point:''+hSp.point):'—');
    return`<div class="line-row"><div class="book-nm">${b.title||b.key}</div><div class="line-val ${oc(aO?.price)}">${fmt(aO?.price)}${bA.book===b.key?'<span class="best-tag">BEST</span>':''}</div><div class="line-val ${oc(hO?.price)}">${fmt(hO?.price)}${bH.book===b.key?'<span class="best-tag">BEST</span>':''}</div><div class="line-val spr">${spD}</div><div class="line-val tot">${over?.point?'O'+over.point:'—'}</div></div>`;
  }).join('');
  return`<div class="game-card" style="animation-delay:${idx*.04}s"><div class="game-teams"><div><div class="team-nm">${away}</div><div class="vs-row">@</div><div class="team-nm">${home}</div></div><div><div class="game-time-disp">${fmtT(game.commence_time)}</div></div></div><div class="line-row hdr"><div class="book-nm">BOOK</div><div class="line-val">${away.split(' ').pop()}</div><div class="line-val">${home.split(' ').pop()}</div><div class="line-val spr">${isMlb?'RL':'SPREAD'}</div><div class="line-val tot">TOTAL</div></div>${rows}<div class="game-footer"><span class="pill">${away.split(' ').pop()}: ${implP(bA.price)||'—'}% imp</span><span class="pill">${home.split(' ').pop()}: ${implP(bH.price)||'—'}% imp</span><span class="pill gold">${bks.length} books</span></div></div>`;
}
// ── BUILD PICKS ────────────────────────────────────
async function buildMLPicks(gs){
  const pitchers=await ensurePitchers();const picks=[];
  for(const game of gs.filter(isPreGame)){
    const home=game.home_team,away=game.away_team;
    const ml=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='h2h').map(m=>({key:b.key,outcomes:m.outcomes})))||[];
    if(ml.length<2)continue;
    const at=getAvgTotal(game),hp=pitchers[home]||null,ap=pitchers[away]||null;
    for(const team of[home,away]){
      const prices=ml.map(b=>(b.outcomes||[]).find(o=>o.name===team)?.price).filter(p=>p!=null&&!isNaN(p));
      if(prices.length<2)continue;
      const max=Math.max(...prices),min=Math.min(...prices),gap=Math.round(max-min);
      const odds=trueAvg(prices);if(!odds)continue;
      const isdog=odds>0,ta=getTA(team,home,sportKey),oppTeam=team===home?away:home,oppTA=getTA(oppTeam,home,sportKey);
      const myP=team===home?hp:ap,oppP=team===home?ap:hp;
      const conf=calcConf(prices,isdog,gap,ta,oppTA,sportKey,!!(hp||ap));
      picks.push({betType:'ML',id:`ml_${game.id||Date.now()}_${team.replace(/\s/g,'_')}`,team,opponent:oppTeam,homeTeam:home,awayTeam:away,type:'ML',odds,conf,isdog,gap,books:prices.length,gameTime:game.commence_time,at,ta,
        lineEdgeTxt:lineEdgeText(prices,gap,isdog,odds,prices.length),
        analyticsTxt:deepAnalyticsText(team,home,ta,oppTA,myP,oppP,sportKey),
        marketTxt:marketText(prices,gap,odds,prices.length,ta),
        matchupHTML:sportKey==='mlb'?pitcherMatchupHTML(hp,ap,home,away):''});
    }
  }
  return picks.sort((a,b)=>b.conf-a.conf);
}

async function buildRLPicks(gs){
  const picks=[];
  for(const game of gs.filter(isPreGame)){
    const home=game.home_team,away=game.away_team;
    const sp=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='spreads').map(m=>({key:b.key,outcomes:m.outcomes})))||[];
    if(!sp.length)continue;
    for(const team of[home,away]){
      const isHome=team===home;
      const prices=sp.map(b=>(b.outcomes||[]).find(o=>o.name===team)?.price).filter(p=>p!=null&&!isNaN(p));
      if(prices.length<2)continue;
      const spreadDisp=sportKey==='baseball_mlb'?(isHome?'−1.5':'+1.5'):(()=>{const pt=sp[0]?.outcomes?.find(o=>o.name===team)?.point||0;return pt>0?'+'+pt:''+pt;})();
      const max=Math.max(...prices),min=Math.min(...prices),gap=Math.round(max-min);
      const odds=trueAvg(prices);if(!odds)continue;
      const isdog=odds>0,ta=getTA(team,home,sportKey),oppTeam=team===home?away:home,oppTA=getTA(oppTeam,home,sportKey);
      const conf=Math.min(74,Math.max(44,calcConf(prices,isdog,gap,ta,oppTA,sportKey,false)-3));
      picks.push({betType:'RL',id:`rl_${game.id||Date.now()}_${team.replace(/\s/g,'_')}`,team,opponent:oppTeam,homeTeam:home,spreadDisp,type:'RL',odds,conf,isdog,gap,books:prices.length,gameTime:game.commence_time,ta,
        lineEdgeTxt:lineEdgeText(prices,gap,isdog,odds,prices.length),
        analyticsTxt:deepAnalyticsText(team,home,ta,oppTA,null,null,sportKey),
        marketTxt:marketText(prices,gap,odds,prices.length,ta)});
    }
  }
  return picks.sort((a,b)=>b.conf-a.conf);
}

async function buildOUPicks(gs){
  const picks=[];
  for(const game of gs.filter(isPreGame)){
    const home=game.home_team,away=game.away_team;
    const tot=game.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='totals').map(m=>({key:b.key,outcomes:m.outcomes})))||[];
    if(!tot.length)continue;
    const overs=tot.map(b=>(b.outcomes||[]).find(o=>o.name==='Over')?.price).filter(p=>p!=null&&!isNaN(p));
    const unders=tot.map(b=>(b.outcomes||[]).find(o=>o.name==='Under')?.price).filter(p=>p!=null&&!isNaN(p));
    const points=tot[0]?.outcomes?.find(o=>o.name==='Over')?.point;
    if(!overs.length||!unders.length||!points)continue;
    const avgOver=trueAvg(overs),avgUnder=trueAvg(unders);if(!avgOver||!avgUnder)continue;
    const ta=getTA(home,home,sportKey),oppTA=getTA(away,home,sportKey);
    const oG=Math.round(Math.max(...overs)-Math.min(...overs)),uG=Math.round(Math.max(...unders)-Math.min(...unders));
    const overConf=Math.min(72,Math.max(44,calcConf(overs,avgOver>0,oG,ta,oppTA,sportKey,false)));
    const underConf=Math.min(72,Math.max(44,calcConf(unders,avgUnder>0,uG,ta,oppTA,sportKey,false)));
    const dir=overConf>=underConf?'over':'under';
    picks.push({betType:'OU',id:`ou_${game.id||Date.now()}`,home,away,total:points,dir,odds:dir==='over'?avgOver:avgUnder,conf:Math.max(overConf,underConf),overOdds:avgOver,underOdds:avgUnder,overConf,underConf,books:overs.length,gameTime:game.commence_time,ta,
      lineEdgeTxt:lineEdgeText(dir==='over'?overs:unders,dir==='over'?oG:uG,false,dir==='over'?avgOver:avgUnder,overs.length),
      analyticsTxt:deepAnalyticsText(home,home,ta,oppTA,null,null,sportKey),
      marketTxt:marketText(dir==='over'?overs:unders,dir==='over'?oG:uG,dir==='over'?avgOver:avgUnder,overs.length,ta)});
  }
  return picks.sort((a,b)=>b.conf-a.conf);
}

// ── RENDER PICK CARD ───────────────────────────────
function renderPickCard(p,i){
  const tier=p.conf>=68?'high':p.conf>=57?'mid':'low';
  const id=p.id||i;
  const tb=p.betType==='RL'?`<span class="type-badge rl">RL ${p.spreadDisp}</span>`:p.betType==='OU'?`<span class="type-badge ${p.dir}">${p.dir==='over'?'OVER':'UNDER'} ${p.total}</span>`:`<span class="type-badge ml">ML</span>`;
  const teamDisp=p.betType==='OU'?(p.dir==='over'?`OVER ${p.total}`:`UNDER ${p.total}`):p.team;
  const subDisp=p.betType==='OU'?`${p.away} @ ${p.home} · ${fmtT(p.gameTime)}`:`${p.betType==='RL'?p.spreadDisp+' · ':''}vs ${p.opponent} · ${fmtT(p.gameTime)}`;
  const safeTeam=(p.betType==='OU'?(p.dir==='over'?`OVER ${p.total}`:`UNDER ${p.total}`):(p.team||'')).replace(/'/g,"\\'");
  return`<div class="pick-card ${tier}" style="animation-delay:${i*.05}s">
  <div class="pick-top">
    <div style="margin-bottom:6px">${tb}</div>
    <div class="pick-header"><div class="pick-team">${teamDisp}</div><div class="conf-badge ${tier}">${p.conf}% ${tier==='high'?'🔥':tier==='mid'?'⚡':'📊'}</div></div>
    <div class="pick-sub">${subDisp}</div>
    <div class="pick-odds-row"><div class="pick-odds-big ${oc(p.odds)}">${fmt(p.odds)}</div><div class="imp-pill">${implP(p.odds)||'—'}% implied</div>${p.at&&p.betType==='ML'?`<div class="imp-pill">O/U ${p.at.toFixed(1)}</div>`:''}</div>
  </div>
  <div class="conf-bar-wrap"><div class="conf-labels"><span>Confidence</span><span>${p.conf}%</span></div><div class="conf-track"><div class="conf-fill ${tier}" style="width:${p.conf}%"></div></div></div>
  <button class="pick-expand-btn" onclick="toggleDetail('${id}')"><span>▼ Full Analysis</span><span style="font-size:9px;color:var(--text3)">${p.books} books · ${p.gap||'—'}pt gap</span></button>
  <div class="pick-detail" id="det_${id}">
    <div class="analysis-section"><div class="analysis-header"><span>📊</span><span class="analysis-title line">LINE EDGE</span></div><div class="analysis-body">${p.lineEdgeTxt||'—'}</div></div>
    ${p.matchupHTML||''}
    <div class="analysis-section"><div class="analysis-header"><span>🔬</span><span class="analysis-title analytics">DEEP ANALYTICS</span></div><div class="analysis-body">${p.analyticsTxt||'—'}</div></div>
    <div class="analysis-section"><div class="analysis-header"><span>📈</span><span class="analysis-title market">MARKET SIGNALS</span></div><div class="analysis-body">${p.marketTxt||'—'}</div></div>
    ${p.betType==='OU'?`<div class="ou-compare"><div class="ou-side over"><div style="font-size:10px;color:var(--text2);margin-bottom:3px">OVER ${p.total}</div><div style="font-size:17px;font-weight:800;color:var(--orange)">${fmt(p.overOdds)}</div><div style="font-size:9px;color:var(--text2)">${p.overConf}% conf</div></div><div class="ou-side under"><div style="font-size:10px;color:var(--text2);margin-bottom:3px">UNDER ${p.total}</div><div style="font-size:17px;font-weight:800;color:var(--purple)">${fmt(p.underOdds)}</div><div style="font-size:9px;color:var(--text2)">${p.underConf}% conf</div></div></div>`:''}
    <div class="stats-row"><div class="stat-box"><div class="val">${fmt(p.odds)}</div><div class="lbl">Best Odds</div></div><div class="stat-box"><div class="val">${implP(p.odds)||'—'}%</div><div class="lbl">Implied</div></div><div class="stat-box"><div class="val">${p.books}</div><div class="lbl">Books</div></div><div class="stat-box"><div class="val">${p.gap||'—'}pt</div><div class="lbl">Gap</div></div></div>
    <div class="quick-bet">
      <div class="qb-label">Log This Bet</div>
      <div class="qb-row">
        <button class="amt-btn" id="ab10_${id}" onclick="selAmt('${id}',10,this)">$10</button>
        <button class="amt-btn" id="ab25_${id}" onclick="selAmt('${id}',25,this)">$25</button>
        <button class="amt-btn" id="ab50_${id}" onclick="selAmt('${id}',50,this)">$50</button>
        <input class="amt-input" type="number" id="ac_${id}" placeholder="$ custom" oninput="selCustom('${id}',this.value)">
        <button class="log-btn" onclick="quickLog('${id}','${safeTeam}','${p.type||p.betType}',${p.odds})">✅ Log</button>
      </div>
      <div class="br-tag">💰 ${brec(p.conf)}</div>
    </div>
  </div>
</div>`;
}
function toggleDetail(id){const el=document.getElementById('det_'+id);if(el)el.classList.toggle('open');}
function selAmt(id,amt,btn){selAmts[id]=amt;document.querySelectorAll(`[id^="ab"][id$="_${id}"]`).forEach(b=>b.classList.remove('on'));if(btn)btn.classList.add('on');const inp=document.getElementById('ac_'+id);if(inp)inp.value='';}
function selCustom(id,val){selAmts[id]=parseFloat(val)||0;document.querySelectorAll(`[id^="ab"][id$="_${id}"]`).forEach(b=>b.classList.remove('on'));}
async function quickLog(id,team,type,odds){const amt=selAmts[id];if(!amt||amt<=0){alert('Select or enter a wager amount first');return;}await logBet(team,type,amt,odds);}

// ── PLAY OF DAY ────────────────────────────────────
async function loadAll(forceRefresh=false){
  const wrap=document.getElementById('potdWrap');
  const cacheKey='gfn_potd_'+new Date().toISOString().split('T')[0];
  // Return cached POTD if same day and not force refresh
  if(!forceRefresh){
    const cached=sessionStorage.getItem(cacheKey);
    if(cached){
      wrap.innerHTML=cached;
      document.getElementById('potdTime').textContent='Today\'s picks (cached)';
      return;
    }
  }
  wrap.innerHTML=`<div class="loading"><div class="spin"></div><p>Scanning all bet types...</p></div>`;
  const gs=await loadOdds();
  if(!gs.length){wrap.innerHTML=`<div class="empty">No games available.</div>`;return;}
  const pregame=gs.filter(isPreGame);
  if(!pregame.length){wrap.innerHTML=`<div class="empty">All games have started. Check back tomorrow for tomorrow's slate.</div>`;// Cache today's POTD so refresh keeps same picks
  const cKey='gfn_potd_'+new Date().toISOString().split('T')[0];
  sessionStorage.setItem(cKey, wrap.innerHTML);
  document.getElementById('potdTime').textContent='Updated '+new Date().toLocaleTimeString()+' · Stable all day';return;}
  const[ml,rl,ou]=await Promise.all([buildMLPicks(gs),buildRLPicks(gs),buildOUPicks(gs)]);
  // Seed by today's date so POTD stays constant all day
  const dateKey=new Date().toISOString().split('T')[0];
  const dateSeed=dateKey.split('-').reduce((a,c)=>a+parseInt(c),0);
  const all=[...ml,...rl,...ou].sort((a,b)=>{
    const diff=b.conf-a.conf;
    if(Math.abs(diff)>2) return diff; // clear winner
    // tie-break with date seed so same picks win all day
    const aH=(a.id||'').split('').reduce((s,c)=>s+c.charCodeAt(0),dateSeed)%100;
    const bH=(b.id||'').split('').reduce((s,c)=>s+c.charCodeAt(0),dateSeed)%100;
    return bH-aH;
  });
  const seen=new Set(),top=[];
  for(const p of all){const key=p.betType==='OU'?`${p.home}|${p.away}`:`${p.homeTeam}|${p.awayTeam}|${p.team}`;if(!seen.has(key)&&top.length<3){seen.add(key);top.push(p);}}
  if(!top.length){wrap.innerHTML=`<div class="empty">No clear plays today.</div>`;return;}
  const typeLabel=p=>p.betType==='RL'?`Run Line ${p.spreadDisp}`:p.betType==='OU'?`${p.dir==='over'?'OVER':'UNDER'} ${p.total}`:'Moneyline';
  const typeColor=p=>p.betType==='RL'?'var(--blue)':p.betType==='OU'?(p.dir==='over'?'var(--orange)':'var(--purple)'):'var(--green)';
  const medals=['🥇','🥈','🥉'];
  wrap.innerHTML=`<div style="font-size:10px;font-weight:700;color:var(--green);letter-spacing:2px;text-transform:uppercase;margin-bottom:14px;">⭐ Top 3 Plays Today · Pre-Game Only · Best Across All Bet Types</div>`+
  top.map((p,i)=>{
    const tc=typeColor(p);
    const tn=p.betType==='OU'?(p.dir==='over'?`OVER ${p.total}`:`UNDER ${p.total}`):(p.betType==='RL'?`${p.team} ${p.spreadDisp}`:p.team);
    const gd=p.betType==='OU'?`${p.away} @ ${p.home}`:`${p.team} vs ${p.opponent}`;
    const ta=p.ta;
    const chips=ta&&sportKey==='mlb'?[{l:'wRC+',v:ta.wRC,pos:ta.wRC>110,neg:ta.wRC<88},{l:'wOBA',v:ta.wOBA,pos:ta.wOBA>0.335,neg:ta.wOBA<0.298},{l:'Barrel%',v:ta.barrelPct+'%',pos:ta.barrelPct>10,neg:ta.barrelPct<7},{l:'K%',v:ta.kPct+'%',neg:ta.kPct>26,pos:ta.kPct<19},{l:'Pen ERA',v:ta.bullpenERA,pos:ta.bullpenERA<3.4,neg:ta.bullpenERA>4.8},{l:'L10',v:`${ta.winsLast10}-${10-ta.winsLast10}`,pos:ta.winsLast10>=7,neg:ta.winsLast10<=3}]:[];
    const chipHtml=chips.length?`<div class="stat-chips">${chips.map(c=>`<div class="stat-chip ${c.pos?'pos':c.neg?'neg':''}">${c.l}: <strong>${c.v}</strong></div>`).join('')}</div>`:'';
    const safeTn=tn.replace(/'/g,"\\'");
    return`<div class="potd-card">
  <div class="potd-glow"></div><div class="potd-rank">${i+1}</div>
  <div class="potd-badge" style="color:${tc}">${medals[i]} ${typeLabel(p)} · Play of the Day</div>
  <div class="potd-game">${gd} · ${fmtT(p.gameTime)}</div>
  <div class="potd-team">${tn}</div>
  <div class="potd-odds ${oc(p.odds)}">${fmt(p.odds)}</div>
  <div class="potd-chips"><span class="pill g">${p.conf}% confidence</span><span class="pill blue">${p.books} books</span><span class="pill gold">💰 ${brec(p.conf)}</span>${p.at&&p.betType==='ML'?`<span class="pill">O/U ${p.at.toFixed(1)}</span>`:''}<span class="pill">${p.gap||'—'}pt gap</span></div>
  <div class="potd-analysis">
    ${chipHtml}
    <div class="potd-section" style="margin-top:${chipHtml?12:0}px">
      <div class="potd-section-label" style="color:var(--blue)"><span>📊</span> LINE EDGE</div>
      <div class="potd-body">${p.lineEdgeTxt}</div>
    </div>
    <div class="potd-section">
      <div class="potd-section-label" style="color:var(--purple)"><span>🔬</span> DEEP ANALYTICS</div>
      <div class="potd-body">${p.analyticsTxt}</div>
    </div>
    <div class="potd-section">
      <div class="potd-section-label" style="color:var(--gold)"><span>📈</span> MARKET SIGNALS</div>
      <div class="potd-body">${p.marketTxt}</div>
    </div>
    ${p.matchupHTML||''}
  </div>
  <div class="potd-log-row">
    <button class="potd-log-btn" onclick="logBet('${safeTn}','${p.type||p.betType}',10,${p.odds})">$10</button>
    <button class="potd-log-btn" onclick="logBet('${safeTn}','${p.type||p.betType}',25,${p.odds})">$25</button>
    <button class="potd-log-btn" onclick="logBet('${safeTn}','${p.type||p.betType}',50,${p.odds})">$50</button>
    <button class="potd-log-btn" onclick="logBetPrompt('${safeTn}','${p.type||p.betType}',${p.odds})">Custom $</button>
  </div>
</div>`;
  }).join('');
  // Cache today's POTD so refresh keeps same picks
  const cKey='gfn_potd_'+new Date().toISOString().split('T')[0];
  sessionStorage.setItem(cKey, wrap.innerHTML);
  document.getElementById('potdTime').textContent='Updated '+new Date().toLocaleTimeString()+' · Stable all day';
}

// ── GENERATE ALL PICKS ─────────────────────────────
async function genAllPicks(){
  document.getElementById('picksTime').textContent='Generating...';
  ['mlWrap','rlWrap','ouWrap'].forEach(id=>document.getElementById(id).innerHTML=`<div class="loading"><div class="spin"></div><p>Loading...</p></div>`);
  if(!games.length)await loadOdds();
  const[ml,rl,ou]=await Promise.all([buildMLPicks(games),buildRLPicks(games),buildOUPicks(games)]);
  document.getElementById('mlWrap').innerHTML=ml.length?ml.slice(0,8).map((p,i)=>renderPickCard(p,i)).join(''):`<div class="empty">No moneyline plays with clear edge today.</div>`;
  document.getElementById('rlWrap').innerHTML=rl.length?rl.slice(0,8).map((p,i)=>renderPickCard(p,i)).join(''):`<div class="empty">No run line plays found.</div>`;
  document.getElementById('ouWrap').innerHTML=ou.length?ou.slice(0,8).map((p,i)=>renderPickCard(p,i)).join(''):`<div class="empty">No over/under plays found.</div>`;
  document.getElementById('picksTime').textContent='Generated '+new Date().toLocaleTimeString();
}

// ── LIVE TAB ───────────────────────────────────────
function bullpenAlert(era,ip){
  const n=parseFloat(ip)||0;
  if(n>=6)return{cls:'safe',txt:`Starter through ${ip} IP — bullpen fresh and rested.`};
  if(n>=4&&parseFloat(era)<4.0)return{cls:'safe',txt:`Starter dealing (${ip} IP today). Bullpen likely fully rested.`};
  if(n<3||parseFloat(era)>5.5)return{cls:'danger',txt:`⚠️ Starter struggling (${ip} IP, ${era} ERA season) — bullpen incoming. Game about to open up.`};
  return{cls:'warning',txt:`Starter at ${ip} IP — mid-game, bullpen warming. Watch for a change.`};
}
function genLiveBetRecs(lg,oddsGame,hp,ap){
  const recs=[];
  const hs=parseInt(lg.homeScore)||0,as=parseInt(lg.awayScore)||0,inn=parseInt(lg.inning)||1,diff=Math.abs(hs-as);
  const ml=oddsGame?.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='h2h').map(m=>({outcomes:m.outcomes})))||[];
  const hO=trueAvg(ml.map(b=>b.outcomes?.find(o=>o.name===lg.home)?.price).filter(p=>p!=null&&!isNaN(p)));
  const aO=trueAvg(ml.map(b=>b.outcomes?.find(o=>o.name===lg.away)?.price).filter(p=>p!=null&&!isNaN(p)));
  if(diff>=5)recs.push({cls:'avoid',type:'AVOID',pick:`Skip ${hs>as?lg.home:lg.away} ML`,reason:`${diff}-run lead in inning ${inn} — line has moved too far. No value remaining.`,odds:null});
  if(diff<=1&&inn>=6){const lead=hs>=as?lg.home:lg.away,lo=hs>=as?hO:aO;if(lo&&lo>-200)recs.push({cls:'live',type:'LIVE ML',pick:lead+' ML',reason:`${diff===0?'Tied game':'1-run game'} heading late (inning ${inn}). Close late games favor the team with fresher bullpen and momentum. ${fmt(lo)} still has value.`,odds:lo});}
  const trailP=hs>as?ap:hp,trailT=hs>as?lg.away:lg.home,trailO=hs>as?aO:hO;
  if(trailP&&parseFloat(trailP.ip)>=5&&trailP.er===0&&diff<5&&trailO&&trailO>100)recs.push({cls:'live',type:'LIVE DOG',pick:trailT+' ML',reason:`${trailP.name} has ${trailP.ip} IP with 0 ER — dealing despite the deficit. Dog line at ${fmt(trailO)} has live +EV.`,odds:trailO});
  if(inn<=2&&hs+as===0)recs.push({cls:'neutral',type:'EARLY INNINGS',pick:'Both starters sharp',reason:`0-0 through ${inn} innings. If NRFI still available on your book, it may still have value.`,odds:null});
  if(!recs.length)recs.push({cls:'neutral',type:'HOLD',pick:'No clear live edge yet',reason:`${lg.away} ${as} – ${lg.home} ${hs}, inning ${inn}. Wait for a situation to develop before placing a live bet.`,odds:null});
  return recs;
}
async function genLive(){
  const wrap=document.getElementById('liveWrap');
  wrap.innerHTML=`<div class="loading"><div class="spin"></div><p>Fetching live MLB data...</p></div>`;
  const[liveGames,allOdds]=await Promise.all([sportKey==='mlb'?fetchLiveMLBGames():Promise.resolve([]),games.length?Promise.resolve(games):loadOdds()]);
  document.getElementById('liveTime').textContent='Updated '+new Date().toLocaleTimeString();
  if(!liveGames.length){wrap.innerHTML=`<div style="text-align:center;padding:48px 16px"><div style="font-size:40px;margin-bottom:12px">⚾</div><div style="font-size:13px;font-weight:600;color:var(--text2);margin-bottom:6px">No live games right now</div><div style="font-size:11px;color:var(--text3)">Games typically start at 1:05, 4:05, 7:05, or 10:05 ET</div></div>`;return;}
  wrap.innerHTML=`<div class="live-header-bar"><div class="live-dot"></div><div class="live-header-txt">${liveGames.length} Game${liveGames.length>1?'s':''} In Progress · Live MLB Data</div></div>`;
  for(const lg of liveGames){
    const oddsGame=allOdds.find(g=>g.home_team===lg.home||g.away_team===lg.away)||null;
    let pitchers=null;try{pitchers=await fetchLivePitcher(lg.gamePk);}catch{}
    const hp=pitchers?.home,ap=pitchers?.away;
    const hTA=getTA(lg.home,lg.home,'mlb'),aTA=getTA(lg.away,lg.home,'mlb');
    const hWin=lg.homeScore>lg.awayScore,aWin=lg.awayScore>lg.homeScore;
    const ml=oddsGame?.bookmakers?.flatMap(b=>b.markets?.filter(m=>m.key==='h2h').map(m=>({outcomes:m.outcomes})))||[];
    const hO=trueAvg(ml.map(b=>b.outcomes?.find(o=>o.name===lg.home)?.price).filter(p=>p!=null&&!isNaN(p)));
    const aO=trueAvg(ml.map(b=>b.outcomes?.find(o=>o.name===lg.away)?.price).filter(p=>p!=null&&!isNaN(p)));
    const hB=bullpenAlert(hp?.seasonERA||hTA.bullpenERA,hp?.ip||'0');
    const aB=bullpenAlert(ap?.seasonERA||aTA.bullpenERA,ap?.ip||'0');
    const bets=genLiveBetRecs(lg,oddsGame,hp,ap);
    const ss=p=>!p?'':p.isStarter?`<span class="live-sit-pill cool">Starter (${p.ip} IP)</span>`:`<span class="live-sit-pill hot">🔄 Bullpen (${p.ip} IP)</span>`;
    const card=document.createElement('div');card.className='live-game-card';
    card.innerHTML=`
<div class="live-game-header">
  <div class="live-scoreboard">
    <div class="live-team-block away"><div class="live-team-name">${lg.away}</div><div class="live-score ${aWin?'win':hWin?'lose':'tie'}">${lg.awayScore}</div>${aO?`<div class="live-odds-line" style="color:${aO>0?'var(--green)':'var(--red)'}">${fmt(aO)} live</div>`:''}</div>
    <div class="live-mid"><div class="live-inning-box"><div class="live-inning-num">${lg.inning}</div><div class="live-inning-lbl">${(lg.inningHalf||'').charAt(0)||'•'}</div></div></div>
    <div class="live-team-block home"><div class="live-team-name">${lg.home}</div><div class="live-score ${hWin?'win':aWin?'lose':'tie'}">${lg.homeScore}</div>${hO?`<div class="live-odds-line" style="color:${hO>0?'var(--green)':'var(--red)'}">${fmt(hO)} live</div>`:''}</div>
  </div>
  <div class="live-sit"><span class="live-sit-pill">${lg.outs} out${lg.outs!==1?'s':''}</span><span class="live-sit-pill">${lg.balls}-${lg.strikes} count</span><span class="live-sit-pill hot">🔴 ${lg.status}</span></div>
</div>
<div class="live-pitcher-section">
  <div style="font-size:9px;font-weight:700;color:var(--text2);text-transform:uppercase;letter-spacing:1px;margin-bottom:8px">⚾ Current Pitchers · Real MLB API Data</div>
  <div class="live-pitcher-row">
    <div class="live-pitcher-block"><div class="live-pitcher-lbl">${lg.away.split(' ').pop()} — Away</div><div class="live-pitcher-name">${ap?.name||'Loading...'}</div>${ss(ap)}<div class="live-p-stats"><span class="live-p-stat">IP: <strong>${ap?.ip||'?'}</strong></span><span class="live-p-stat">ER: <strong>${ap?.er??'?'}</strong></span><span class="live-p-stat">K: <strong>${ap?.k??'?'}</strong></span><span class="live-p-stat">PC: <strong>${ap?.pc??'?'}</strong></span></div><div style="font-size:9px;color:var(--text2);margin-top:4px">Season ERA: <strong>${ap?.seasonERA||aTA.bullpenERA}</strong> · WHIP: <strong>${ap?.seasonWHIP||'—'}</strong></div><div class="bullpen-alert ${aB.cls}">${aB.txt}</div></div>
    <div class="live-pitcher-block"><div class="live-pitcher-lbl">${lg.home.split(' ').pop()} — Home</div><div class="live-pitcher-name">${hp?.name||'Loading...'}</div>${ss(hp)}<div class="live-p-stats"><span class="live-p-stat">IP: <strong>${hp?.ip||'?'}</strong></span><span class="live-p-stat">ER: <strong>${hp?.er??'?'}</strong></span><span class="live-p-stat">K: <strong>${hp?.k??'?'}</strong></span><span class="live-p-stat">PC: <strong>${hp?.pc??'?'}</strong></span></div><div style="font-size:9px;color:var(--text2);margin-top:4px">Season ERA: <strong>${hp?.seasonERA||hTA.bullpenERA}</strong> · WHIP: <strong>${hp?.seasonWHIP||'—'}</strong></div><div class="bullpen-alert ${hB.cls}">${hB.txt}</div></div>
  </div>
</div>
<div class="live-bets-section">
  <div class="live-bets-hd">🎯 Live Bet Recommendations</div>
  ${bets.map(r=>`<div class="live-bet-card ${r.cls}"><div class="live-bet-type">${r.type}</div><div class="live-bet-pick">${r.pick}</div><div class="live-bet-reason">${r.reason}</div>${r.odds?`<div class="live-bet-odds ${oc(r.odds)}">${fmt(r.odds)}</div>`:''} ${r.cls!=='avoid'&&r.odds?`<button class="live-log-btn" onclick="logBetPrompt('${r.pick.replace(/'/g,"\\'")}',${r.odds})">✅ Log Live Bet</button>`:''}</div>`).join('')}
</div>`;
    wrap.appendChild(card);
  }
}

// ── NRFI / YRFI ────────────────────────────────────
async function genNRFI(){
  const wrap=document.getElementById('nrfiWrap');
  wrap.innerHTML=`<div class="loading"><div class="spin"></div><p>Analyzing first innings...</p></div>`;
  if(!games.length)await loadOdds();
  await ensurePitchers();
  const analyses=games.map(game=>{
    const home=game.home_team,away=game.away_team,at=getAvgTotal(game);
    const hp=mlbPitchers[home]||null,ap=mlbPitchers[away]||null;
    const hERA=hp?.stats?parseFloat(hp.stats.era):null,aERA=ap?.stats?parseFloat(ap.stats.era):null;
    let score=50,lR='',aR='',rec='s',recText='⚠ Skip — No Strong Signal';
    if(at){
      if(at<=7.0){score+=20;lR=`O/U <strong>${at.toFixed(1)}</strong> — pitcher's duel expected. <span class="pos">Strong NRFI lean.</span>`;rec='n';recText='✅ Lean NRFI';}
      else if(at<=8.0){score+=10;lR=`O/U <strong>${at.toFixed(1)}</strong> — pitcher-friendly total. Soft NRFI lean.`;rec='n';recText='📊 Soft NRFI';}
      else if(at>=10.0){score-=20;lR=`O/U <strong>${at.toFixed(1)}</strong> — hitter-friendly. <span class="pos">Strong YRFI lean.</span>`;rec='y';recText='✅ Lean YRFI';}
      else if(at>=9.0){score-=10;lR=`O/U <strong>${at.toFixed(1)}</strong> — leans high-scoring. Soft YRFI lean.`;rec='y';recText='📊 Soft YRFI';}
      else lR=`O/U <strong>${at.toFixed(1)}</strong> — neutral total. No clear lean.`;
    }else lR='No total data available.';
    if(hERA!==null&&aERA!==null){
      const avg=(hERA+aERA)/2,hN=hp?.name||'Home SP',aN=ap?.name||'Away SP';
      const hAdv=hp?.stats?calcFIP(hp.stats):null,aAdv=ap?.stats?calcFIP(ap.stats):null;
      if(avg<3.5){score+=15;aR=`<span class="pos">Both aces dealing</span> — ${aN} (ERA ${aERA}${aAdv?', FIP '+aAdv.fip:''}) vs ${hN} (ERA ${hERA}${hAdv?', FIP '+hAdv.fip:''}). Sub-3.5 avg ERA starters suppress 1st innings at elite rates.`;}
      else if(avg>4.5){score-=15;aR=`<span class="neg">Leaky starters</span> — ${aN} (${aERA}) vs ${hN} (${hERA}). High-ERA arms give up leadoff hits at elevated rates. YRFI hit rate climbs significantly.`;}
      else aR=`Average starters — ${aN} (ERA ${aERA}) vs ${hN} (ERA ${hERA}). Lean on the total line over pitcher data here.`;
    }else aR='Probable pitcher data unavailable. Check your sportsbook before betting NRFI/YRFI.';
    const ns=Math.min(84,Math.max(20,score));
    return{home,away,at,ns,ys:100-ns,lR,aR,rec,recText,gameTime:game.commence_time,hp,ap};
  });
  analyses.sort((a,b)=>Math.abs(b.ns-50)-Math.abs(a.ns-50));
  wrap.innerHTML=analyses.map((a,i)=>`<div class="nrfi-card" style="animation-delay:${i*.06}s">
  <div class="nrfi-game">${a.away} @ ${a.home}</div>
  <div class="nrfi-meta">${fmtT(a.gameTime)}${a.ap?' · SP: '+a.ap.name:''}${a.hp?' vs '+a.hp.name:''}</div>
  <div class="nrfi-bars">
    <div class="nrfi-bar-wrap"><div class="nrfi-bar-label"><span style="color:var(--green)">NRFI</span><span>${a.ns}%</span></div><div class="nrfi-track"><div class="nrfi-fill n" style="width:${a.ns}%"></div></div></div>
    <div class="nrfi-bar-wrap"><div class="nrfi-bar-label"><span style="color:var(--blue)">YRFI</span><span>${a.ys}%</span></div><div class="nrfi-track"><div class="nrfi-fill y" style="width:${a.ys}%"></div></div></div>
  </div>
  <div class="analysis-section" style="padding:0 0 10px"><div class="analysis-header"><span>📊</span><span class="analysis-title line">LINE EDGE</span></div><div class="analysis-body">${a.lR}</div></div>
  <div class="analysis-section" style="padding:8px 0 0;border-bottom:none"><div class="analysis-header"><span>⚾</span><span class="analysis-title analytics">PITCHING ANALYTICS</span></div><div class="analysis-body">${a.aR}</div></div>
  <div class="nrfi-rec ${a.rec}">${a.recText}</div>
</div>`).join('');
  document.getElementById('nrfiTime').textContent='Analyzed '+new Date().toLocaleTimeString();
}

// ── GOBLINS ────────────────────────────────────────
const GOBLINS=[{player:'Shohei Ohtani',team:'LAD',stat:'Hits+Runs+RBIs',line:1.5,dir:'OVER',rate:74,why:'Just needs 1 hit and 1 run or RBI. Hits 1.4+/game and scores constantly in that Dodgers lineup.'},{player:'Mookie Betts',team:'LAD',stat:'Hits',line:0.5,dir:'OVER',rate:82,why:'One hit from a .310+ hitter. The easiest line on the board — averages 1.4+ hits per game.'},{player:'Aaron Judge',team:'NYY',stat:'Total Bases',line:1.5,dir:'OVER',rate:71,why:'Exit velocity alone makes 1.5 TB reliable. One single + stolen base or one XBH cashes.'},{player:'Gunnar Henderson',team:'BAL',stat:'Hits+Runs+RBIs',line:1.5,dir:'OVER',rate:68,why:'Heart of the Orioles order. Hits for average and power — any productive AB cashes.'},{player:'Freddie Freeman',team:'LAD',stat:'Hits',line:0.5,dir:'OVER',rate:79,why:'Safest goblin on the board. .310+ career hitter at top of the best lineup in baseball.'},{player:'Yordan Alvarez',team:'HOU',stat:'Total Bases',line:1.5,dir:'OVER',rate:70,why:'Elite power — one double or any HR cashes. 1.8 TB/game rate this season.'},{player:'Juan Soto',team:'NYM',stat:'Hits+Runs+RBIs',line:1.5,dir:'OVER',rate:67,why:'Elite OBP means he reaches base constantly. Any hit + run or RBI easily cashes this combo.'}];
function genGoblins(){
  const shuffled=[...GOBLINS].sort(()=>Math.random()-.5).slice(0,5);
  document.getElementById('goblinWrap').innerHTML=shuffled.map((g,i)=>`<div class="goblin-card" style="animation-delay:${i*.07}s"><div class="goblin-badge">🎰 Goblin Alert · ${g.rate}% hit rate</div><div class="goblin-player">${g.player}</div><div class="goblin-line">${g.dir} <strong>${g.line} ${g.stat}</strong> · ${g.team}</div><div class="rate-row"><span>Hit Rate</span><span style="color:var(--gold);font-weight:700">${g.rate}%</span></div><div class="goblin-track"><div class="goblin-fill" style="width:${g.rate}%"></div></div><div style="font-size:12px;color:var(--text2);line-height:1.65">${g.why}</div><div class="br-tag" style="margin-top:10px">💰 ${brec(g.rate)}</div></div>`).join('');
  document.getElementById('goblinTime').textContent='Updated '+new Date().toLocaleTimeString();
}

// ── TRACKER ────────────────────────────────────────
async function logBet(name,type,wager,odds){const bets=await getBets();bets.unshift({id:Date.now(),name,type,wager,odds,result:null,date:new Date().toLocaleDateString()});await saveBets(bets);renderTracker();alert(`✅ Logged: ${name} ${type} $${wager} at ${fmt(odds)}`);}
async function logBetPrompt(name,odds){const w=parseFloat(prompt(`Wager amount for "${name}":`)||'0');if(!w||w<=0)return;await logBet(name,'LIVE',w,odds);}
async function addBet(){
  const name=document.getElementById('bName').value.trim(),type=document.getElementById('bType').value,wager=parseFloat(document.getElementById('bWager').value),odds=parseInt(document.getElementById('bOdds').value);
  if(!name||!wager||!odds){alert('Fill in all fields to log a bet');return;}
  await logBet(name,type,wager,odds);['bName','bWager','bOdds'].forEach(id=>document.getElementById(id).value='');
}
async function setResult(id,result){
  const bets=await getBets(),bet=bets.find(b=>b.id===id);if(!bet)return;
  const wasLoss=bet.result==='loss';bet.result=result;await saveBets(bets);
  if(result==='loss'&&!wasLoss){const losses=await getLosses();losses.unshift({id:bet.id,name:bet.name,type:bet.type,odds:bet.odds,wager:bet.wager,date:bet.date,note:''});await saveLosses(losses);}
  renderTracker();
}
async function delBet(id){await saveBets((await getBets()).filter(b=>b.id!==id));renderTracker();}
async function renderTracker(){
  const bets=await getBets();
  const settled=bets.filter(b=>b.result&&b.result!=='push');
  const wins=settled.filter(b=>b.result==='win'),losses=settled.filter(b=>b.result==='loss');
  let pnl=0;wins.forEach(b=>{pnl+=parseFloat(pay(b.wager,b.odds));});losses.forEach(b=>{pnl-=b.wager;});
  const wr=settled.length?((wins.length/settled.length)*100).toFixed(0):0;
  const pEl=document.getElementById('pnlVal');if(!pEl)return;
  pEl.textContent=(pnl>=0?'+':'')+'$'+Math.abs(pnl).toFixed(2);pEl.className='pnl-num '+(pnl>0?'pos':pnl<0?'neg':'neu');
  const wEl=document.getElementById('wrVal');wEl.textContent=wr+'%';wEl.className='pnl-num '+(parseInt(wr)>=55?'pos':'neu');
  document.getElementById('betsVal').textContent=bets.length;
  document.getElementById('betList').innerHTML=!bets.length?`<div class="empty">No bets logged yet</div>`:bets.map(b=>`<div class="bet-row"><div class="bet-left"><div class="bet-nm">${b.name}</div><div class="bet-meta">${b.type} · ${fmt(b.odds)} · ${b.date}${b.type==='LIVE'?' 🔴':''}</div></div><div class="bet-pay">$${b.wager} → <span>+$${pay(b.wager,b.odds)}</span></div><div class="bet-actions"><button class="res-btn w ${b.result==='win'?'on':''}" onclick="setResult(${b.id},'win')">W</button><button class="res-btn l ${b.result==='loss'?'on':''}" onclick="setResult(${b.id},'loss')">L</button><button class="res-btn p ${b.result==='push'?'on':''}" onclick="setResult(${b.id},'push')">P</button><button class="del-btn" onclick="delBet(${b.id})">✕</button></div></div>`).join('');
}

// ── LEARN ──────────────────────────────────────────
async function updateNote(id,note){const ls=await getLosses(),f=ls.find(x=>x.id===id);if(f){f.note=note;await saveLosses(ls);}}
async function renderLearn(){
  const[losses,bets]=await Promise.all([getLosses(),getBets()]);
  document.getElementById('lossLog').innerHTML=!losses.length?`<div class="empty">No losses yet — stay green 🟢</div>`:losses.map(l=>`<div class="loss-card"><div class="loss-hd">❌ Loss — ${l.date}</div><div class="loss-body"><strong>${l.name}</strong> · ${l.type} · ${fmt(l.odds)} · $${l.wager}</div><input class="note-field" type="text" placeholder="Why did this lose? Be honest." value="${l.note||''}" onchange="updateNote(${l.id},this.value)"></div>`).join('');
  const settled=bets.filter(b=>b.result);
  if(settled.length<5){document.getElementById('patternLog').innerHTML=`<div class="empty">Need ${5-settled.length} more settled bets to analyze patterns</div>`;return;}
  const byType={};settled.forEach(b=>{if(!byType[b.type])byType[b.type]={w:0,l:0};if(b.result==='win')byType[b.type].w++;else if(b.result==='loss')byType[b.type].l++;});
  const patterns=Object.entries(byType).filter(([,d])=>d.w+d.l>=2).map(([type,d])=>{const tot=d.w+d.l,wr=((d.w/tot)*100).toFixed(0);return{type,wr:parseInt(wr),w:d.w,l:d.l};});
  document.getElementById('patternLog').innerHTML=!patterns.length?`<div class="empty">Not enough data per type yet — keep logging</div>`:patterns.map(p=>`<div class="pattern-card"><div style="font-size:10px;font-weight:700;color:${p.wr>=55?'var(--green)':p.wr<40?'var(--red)':'var(--gold)'};text-transform:uppercase;margin-bottom:6px">${p.type} — ${p.wr}% win rate (${p.w}W / ${p.l}L)</div><div style="font-size:12px;color:var(--text2)">${p.wr>=55?'Your best bet type — this is where your edge lives. Keep firing.':p.wr<40?'Bleeding on this type. Cut it or only take 70%+ confidence plays.':'Breaking even. Only touch 68%+ confidence plays in this category.'}</div></div>`).join('');
}
</script>
</body>
</html>
